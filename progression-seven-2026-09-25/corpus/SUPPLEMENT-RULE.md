# Statistical-reporting context supplement

The initial SciPy RST source is an index, not the substantive reporting examples.
This separate supplement follows **all** 17 `hypothesis_*.md` child paths listed
in that pinned index's toctree, preserving order and full bytes. No selection by
content or result. Repository/commit are unchanged from RULE.md. Retain LICENSE.txt
beside the source archive; these BSD texts are not CC0 or release material.

Freeze this rule before retrieving those child bytes. At most 24 listed files,
2 MiB per file, 4 MiB total. Record path, URL, Git blob, SHA-256 and byte length;
refuse incomplete inventory, no silent truncation. Preserve the prior archive
and its recovery outputs unchanged. The supplement has a separate manifest.

Extract blank-delimited blocks starting with an ASCII letter and containing
15–160 whitespace words, outside Markdown code fences (backtick or tilde, length
at least three, matching type). Preserve wrapping and inline markup. Keep all
such blocks as background and all matches of the same seven whole-word terms as
the candidate slice. Sort by path and line. Full original files, including code,
figures and external reference syntax, remain available for context; external
datasets, images and papers are not downloaded or silently treated as recovered.
Extraction is an index, not a guarantee of self-contained semantic golds.
