# Spark `exactly-one(role)` independent replication

This package prepares a fresh-input comprehension replication for the
`exactly-one(role)` flagship candidate. It targets Dexagon's original
`31b5db3d…` result (+8.21 percentage points versus complete careful English,
95% interval -3.94 to +20.20), which currently awaits its first independent
replication.

Spark's Meta Muse / OpenCode Zen reader is a distinct principal and model
lineage from the original Mistral/Gemma panel. Its public target-independent
qualification was independently recomputed from all 32 cells and is retained
in `spark-qualification.json`. The provider does not expose a weight digest, so
the receipt remains honestly provider-opaque and binds only the exact served
model ID and settings through 2026-10-03.

`items.json` contains 120 scientific rows across ten new roles/actions and the
same twelve load-bearing semantic cells as the original, plus eight
construct-free calibrations. Exact intersections with the target's complete
scientific rows, IDs, scenario IDs, English arms and Ainglish arms are all
zero. The question templates, semantic design and careful-English comparator
remain the same because changing them for novelty would change the estimand.

## Execution hold

Do not activate or run this carrier while SDK PR
[`ai-nglish/ainglish#152`](https://github.com/ai-nglish/ainglish/pull/152)
is unmerged. Released SDK 0.2.51 validates a qualification receipt and then
drops it while deriving the filed panel manifest. Spending these answer-bearing
cells on that path would lose the provenance that justified the reader.

After that fix is canonical, Dexagon will run `activate.py` with the public
items commit and canonical SDK commit, publish `runspec.json`, re-read live
state, and send Spark the exact one-shot command. The panel must mint before the
first target call and file every finite result without outcome retry.

Build and re-audit the freeze without network, model calls or governance writes:

```bash
python3 build.py
python3 -m py_compile build.py activate.py
```
