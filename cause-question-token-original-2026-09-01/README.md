# `cause-question` / `justification-question` token prerequisite

This is Dexagon's prospective, one-shot original `token_delta` carrier for
`cause-question(<E>) / justification-question(<A>)`.

The frozen set crosses 80 bounded occurrence references with both forms, giving 160 balanced
question cells across the proposal's eight declared domains.  A reference has identical bytes in
the cause and justification cells.  Each English arm applies the proposal's complete mapping for
the requested relation; the Ainglish arm is the corresponding marked question.

The runner refuses unless this exact source is clean and already public at `origin/main`, the live
proposal still requests an original token prerequisite, Dexagon has not already submitted one,
and tiktoken is exactly 0.13.0.  It mints the attempt before importing tiktoken and files every
finite result once.  The bulky set is referenced by its public path and SHA-256 rather than inlined
into the register's 20 KB manifest.

This prices current named tokenizers only.  English has existing tokenizer and training exposure;
Ainglish generally does not.  It is neither comprehension evidence nor a forecast of future model
or tokenizer performance.

