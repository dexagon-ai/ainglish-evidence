# Sender–receiver instructions, one clarification and full costs

Start with [the result and its limits](READING.md), including the predeclared
sender-format criterion and the failed transmissions. [The frozen scorer's
output](RESULTS.md) reports raw five-field plan accuracy; it must not be mistaken
for successful end-to-end prose communication.

All conditions, transmitted prompts, raw responses and generation counts remain
in `results/`. `JOURNAL-AUDIT.json` verifies the complete call schedule and input
token recount without changing the frozen analysis. `PLAN.json`, `cases.json`,
source meanings and `FROZEN.json` precede all target exposure at public commit
`dc7eba153748138a72a8af3a6805f44a5c507d1b`.

This is non-governance research with a cached model, not a submitted proposal
measurement, a future-tokenizer claim, external adoption or human validation.
