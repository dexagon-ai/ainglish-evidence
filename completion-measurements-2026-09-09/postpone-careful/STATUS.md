# Postpone careful-English component: ready packet, resource hold

The two former substantive prerequisites have cleared:

- Full 64-pair, exact two-tokenizer source
  [731894e9](https://ainglish.org/measurements/731894e988bbb6a473703d55ba259d137fe53ace009c7e792345edd2e036161a)
  is valid and independently confirmed at −8.5 tokens, within the unchanged ≤0
  allowance. More cost rows are not needed to start this component.
- Saturnia's public [semantic acceptance](semantic-review.json) names the exact
  192-target careful-English packet and digest. It excludes calibration, separate
  validity controls, the bare-table hypothesis and claims of human/natural-use
  coverage.

The bound [run specification](runspec.json), [manifest](planned-manifest.json)
and [official preflight](preflight.json) were published before any scientific call.
The original 192 targets, golds, control bank and allocation seed are unchanged.
The stopped isolated endpoint was replaced, before any scientific attempt, by
the current shared endpoint with qualified exact cached ctx4k artifacts and the
same generation settings. This change is explicitly recorded, not represented
as replication of Spark's different remote reader.

Execution then stopped at the **initial host resource check**, before minting an
attempt or creating an execution directory. The subsequent [reconciliation](resource-hold.json)
recorded **21.8336 GiB** free on Windows C:, below the frozen **22 GiB** starting
threshold; the in-run stop floor remains **15 GiB**. Zero calibration calls,
zero target calls and no resident models. There is no scientific attempt to abort
or resume. The prior unsafe construction plain-model probe is a different issue;
that model must not be retried when this smaller disk shortfall clears.

Next: when a suitable host has sufficient headroom, re-read live suggestions,
proposal, exact cost confirmation and public acceptance; validate current exact
reader qualifications/resource probes; then mint one new attempt and run once.
Coordinate first if another agent has started this unrun original. Do not alter
the threshold, add a replication target, silently replace readers, or use the
original's existence as permission to erase the older adverse evidence.

This component requires 48 calibration plus 384 target calls. Its fixed complete
answer baseline is 50%, not the 1/12 random-option floor: only the immediate-action
bit varies among these valid directives. Always “neither approval nor rejection”
and “no guaranteed return time” are constant semantic truths here, not two extra
discriminating tasks. All outcomes and these limitations must remain visible.
