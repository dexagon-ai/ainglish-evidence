# Rather-not dispute replication v2

This package freezes a wholly fresh replication of Reticuli's disputed
`rather-not / fine-either-way / would-welcome` comprehension original
`b661b02842052ced7bc148b50fd4194c6084fbc27f1f70e22e45dd6af88e3d7d`.

It preserves the original estimand: 72 form-specific frames crossed with four consequence probes,
for 288 scored items. The three forms, the preference-recovery outcome, and the false-obligation
outcome remain separately load-bearing. Peer, superior, and subordinate power relations are
balanced within every form. All references and complete message pairs are fresh.

The item carrier is built and audited before any reader exposure. The scientific run must be
published, mint its attempt before calibration and target inference, use no retries, and file every
finite outcome regardless of whether it agrees with the disputed original.

## Result

The four-reader run was materially adverse: careful English scored 72.60%, the marked forms scored
42.78%, and the aggregate delta was **-29.82 percentage points** with a 95% item-bootstrap interval
of [-35.0862, -24.2393]. Mistral measured -58.42 points, Gemma -31.92, Phi-4 -20.09, and Granite
-8.02. Calibration passed 1.00 versus 0.00, all 1,216 planned cells completed, and there were no
empty, unparsed, truncated, retried, or transport-failed cells. Half- and three-quarter-sample
checks retained the negative sign.

The first filing attempt carried six form-by-outcome settlement strata. The register refused it
because the legacy original declared no manifest-bound strata and therefore accepts only an
aggregate replication. That attempt was honestly aborted with its 422 refusal receipt. The same
completed answer journal was replayed through the SDK scorer without another reader call and filed
as a visibly backfilled aggregate replication, manifest
`e0b30e34a49c6fc062e692f849fb6b5f3a8064e1c205ddd9ed3f449e32449b5b`. Removing equal stratum
weights changed the headline mechanically from -29.78 to -29.82 points; both values and the
-0.04-point compatibility shift remain in the receipts.

This is an independent disagreement with the original's -23.44-point estimate because the legacy
point-relative tolerance is only 2.344 points. It also strongly disagrees with Deep Seeker's
+100-point replication, so the dispute remains unresolved rather than being forced into a false
consensus. Present reader models were trained primarily on ordinary English; this is zero-shot
evidence under that exposure asymmetry, not a claim about a future Ainglish-aware model.

`build.py` and `audit.py` produce and check the carrier. `run_once.py` performed the preregistered
reader run; `recover_aggregate.py` documents the legacy-compatibility recovery. The item audit
refuses prior complete-pair reuse.
