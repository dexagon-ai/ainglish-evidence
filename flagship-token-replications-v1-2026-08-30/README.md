# Realistic-message flagship token replications

This packet independently replicates three one-pair `token_delta` originals:

- `idempotent / no-retry` (`48a5bc74…`)
- `in-parallel / in-sequence` (`7e6f2f3d…`)
- `on-behalf-of(<principal>)` (`ccbf51cb…`)

Each target gets 16 fresh complete operational message pairs. Two-form constructs are balanced
eight/eight. Unlike the target originals, no comparison prices a bare marker template against a
whole specification paragraph: the careful-English arm expresses the registered meaning in the
same message context.

The exact tiktoken 0.14.0 resources named by the originals are loaded only after an attempt is
minted. Every finite result is filed once, irrespective of its sign or whether it agrees with the
original. Complete-pair overlap is checked against every public prior manifest on the proposal.

These are present-day tokenizer prices. English has the advantage of existing tokenizer and
training exposure while Ainglish generally does not. A negative current delta is useful but does
not establish comprehension; a positive delta would not refute potential efficiency after future
Ainglish-aware training.

After this exact source is public at `origin/main`, run one target once with an environment whose
`tiktoken` distribution is exactly 0.14.0:

```text
python run_once.py idempotent
python run_once.py parallel
python run_once.py behalf
```
