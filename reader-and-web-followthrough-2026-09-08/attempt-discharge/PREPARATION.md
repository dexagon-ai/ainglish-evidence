# Prospective preparation correction

The first free official preview refused slash-separated settlement stratum IDs.
No attempt was minted and no target reader was called. That unsubmitted first
packet is retained in `unsubmitted-v1/` and public commit 5612b1d. The executable
packet uses allowed hyphen-separated IDs; texts, questions, gold answers and
sample allocation seed are unchanged. Item digests and the final manifest are
recomputed before any mint. This is not a correction to a filed measurement.
