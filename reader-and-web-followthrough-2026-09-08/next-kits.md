# Postpone and replacement programmes

Two **192-scenario, ledger-first careful-English packets** are retained in `postpone-kit/` and `replace-kit/`. Exact reader plans with target-independent calibration are in `postpone-careful-v2/` and `replace-careful/`. Each programme also has 24 prescribed invalid/carve-out fixtures plus 24 valid applicability controls, preventing an always-reject strategy from passing the separate validity diagnostic.

The first postpone dry preview refused whitespace in two stratum identifiers. No attempt or reader call occurred. `postpone-careful/` remains the unsubmitted v1; v2 changes only the identifier spelling and resulting metadata digests, not the visible text, golds, seed or readers. Review/execute v2, not v1. This is a preparation refusal, not an aborted experiment or an adverse language result.

These are frozen prospective preparations, **not completed measurements**. The named full-coverage cost originals must be independently confirmed first. Small older cost studies or the author's own recount do not substitute for that confirmation. Then obtain a bounded independent semantic review covering the exact input digest, not agreement with the hoped-for result. Both existing readers are already cached; no download is needed.

After a genuine review, record its reviewer, public URL and exact reviewed `items_sha256` mapping in `next-kit-semantic-review.json`. Do not create a fabricated acceptance file. The runner deliberately refuses to proceed without it. Recheck the unchanged proposal and current cost states before each study. For an approved packet:

```bash
python reader-and-web-followthrough-2026-09-08/run_reader.py prepare postpone-careful-v2
# Independently inspect the preflight; publish the bound runspec/manifest before continuing.
python reader-and-web-followthrough-2026-09-08/run_reader.py run postpone-careful-v2
```

Replace the packet name with `replace-careful`, `postpone-validity` or `replace-validity` as appropriate. This local runner uses the established authenticated helper; no credentials belong in a packet or conversation. An independent participant must bind their own exact qualified instrument and identity and freeze a fresh protocol-equivalent sample, not reuse Dexagon's receipts as if they were theirs.

## Coverage and honest limits

Postpone crosses two forms, eight domains, speaker/reader variety labels, delivery and stated/unpinned procedural effect. It asks what the current instruction requests and asserts, not an unsupported claim about a past vote. Its careful-English comparison cannot establish the separate +30pp gain against bare `table`. Bare `table` with genuinely unstated dialect can be studied as an interpretation choice or intended-action communication task, but a private ledger does not make that intended meaning linguistically recoverable. A reviewer/author must settle that estimand distinction before the bare comparison is called comprehension accuracy. At least 144 truly cross-dialect-live cases still require admission review. Named-rule nulls and a precise corruption schedule remain distinct components, not silently satisfied by these 192 cases.

Replacement crosses eight domains, four forces and both nearby reference orders. Its ledger distinguishes a requested/proposed/quoted action from a completed report, while the role question is explicitly conditional on completion. The common record currently says that no deletion, exchange, compatibility or authority facts are supplied: excluded-inference findings therefore apply **with that safeguard**, not to an unaided cold marker. An independent reviewer may ask for a new prospectively frozen unsafeguarded sensitivity packet; do not change these bytes after exposure. Report leaving/entering/occupant bits separately from the four excluded inferences. The >=92% role target is not the same as complete seven-bit-vector accuracy.

The validity studies use identical text in both assigned arms deliberately; the meaningful results are absolute valid/invalid classifications, not an arm-delta claim. Repeated defect templates are labelled as such. Further bare-wording and damaged-form studies remain supplementary work, not a reason to overstate completion of the current claim.

The existing adverse postpone reader result (-28.57pp, 14 targets) and the old replacement result (-2.94pp, 32 targets) remain in the register. The new packets do not erase, repair or independently confirm them.
