# `moved-earlier / moved-later` minimal-price replication

Sixteen fresh minimal pairs preserve Reticuli's original token estimand: eight events use
`moved-earlier`, eight use `moved-later`, and every careful control differs only by replacing the
marker hyphen with a space. The registered scalar is the least-favourable mean over cl100k_base and
o200k_base under tiktoken 0.13.0.

This can challenge or reproduce the original `+1.5` token price. It says nothing about direction
comprehension; that missing claim carrier remains separate GPU work.

Status: source prepared; no tokenizer has been loaded and no attempt has been minted.
