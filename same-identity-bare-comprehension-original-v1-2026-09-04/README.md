# `same-one / same-kind / same-name` bare-`same` resolving original

This 48-frame, 96-question carrier asks the disambiguation question left open by the existing
ceiling-level careful-English measurement. Every fresh frame compares one marked identity form
with bare `same`; the hidden ledger fixes whether the intended relation is numerical identity,
verified equality under a named check at a named moment, or name match only.

Each form has 32 load-bearing questions. Propagation probes distinguish one shared object from
separate objects. Relation-basis probes require the exact one/kind/name category. Four same-kind
fixtures explicitly refuse relation laundering: equality under a normalized or semantic check
does not establish byte equality. Sixteen unrelated items calibrate the two qualified reader
lineages before any scientific cell.

The scalar is current zero-shot marked-minus-bare exact-answer accuracy. Current readers have an
ordinary-English training advantage and are not assumed to have seen Ainglish; the result does not
cap future performance after corpus exposure or tokenizer adaptation. Every finite direction will
be filed once, and an independent fresh replication remains required for confirmation.

## Result

Attempt `96d3faff-25e9-4f05-a66d-e13c319440e1` filed manifest `ec564448…` with
all 256 planned cells, passing target-independent calibration, no transport faults, and no retries.
The marked arm scored 62.23% versus 34.04% for bare `same`: **+28.1867 percentage points**
(95% item-bootstrap interval **+18.1467 to +38.4640**). Both reader lineages were positive
(+40.7933 and +16.14 points), and downsampling stayed inside the interval.

The form-level result is mixed and must constrain any flagship claim:

- `same-one`: 89.66% marked versus 85.71% bare, +3.95 points. Bare `same` already defaults strongly
  to numerical identity in these shared-edit frames.
- `same-kind`: 69.44% marked versus 10.71% bare, +58.73 points. The named check and moment recover a
  distinction that bare `same` loses.
- `same-name`: 27.59% marked versus 5.71% bare, +21.88 points. The relative gain is positive, but
  absolute marked accuracy is below chance and is not acceptable evidence of present transparency.

Thus the three-way construct improves exact recovery in aggregate, but this original does not make
the current `same-name` surface flagship-ready. Seek an independent exact-contract replication; if
the weak absolute `same-name` cell persists, test a clearer surface or amend the form rather than
advancing on the pooled delta alone. The existing careful-English non-inferiority result remains
visible and answers a different question.
