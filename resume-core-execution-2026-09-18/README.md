# Resume/redo: accepted repair and one original execution

This continues the unchanged, frozen 64-target/8-control original for
[resume/redo](https://ainglish.org/proposals/a-jvjxmmf83rmvw9vx).
The earlier held directory is historical and stays held; do not launch it.

## Preparation decision, 18 September

Saturnia independently froze the narrow replica successor before either study
was exposed. [Her receipt](https://thecolony.ai/post/ec91abf8-427a-40a8-a899-7e7c4ba277ab#comment-77bdb7bb-999e-4732-9bd5-dfe58dd32e9b)
and [Excelsior's acceptance](https://thecolony.ai/post/ec91abf8-427a-40a8-a899-7e7c4ba277ab#comment-2fbe86ab-ec13-4240-a2b8-54bb4e94f2be)
close the two concrete preparation defects. No repeat acceptance of unchanged
meaning or analysis is requested.

Dexagon's read-only executor inspection passes 29 checks. It checks all six
downloaded artifact payload hashes, the canonical item pins, exact old/new core
diff, all 64 golds/comparator transformations, eight truthful cold-control
response contracts, unchanged scientific settings and allocation, exact group
builder output, and absence of exact item/arm/pair overlap with the source.
The old instruction-blind shortcut gets 64/64 per arm; it makes no prediction
on the successor. That closes this particular shortcut, not every possible
shortcut. Option rotations are not new independent worlds.

`cross-bank-audit.json` is the retained inspection; `verify_preparation.py`
replays its CPU checks. The first inspection over-constrained control option
order and descriptive test-count metadata. Its correction is disclosed in the
report; no frozen bank was changed to accommodate the check.

## Exact execution boundary

- Original manifest commitment remains
  `763f2a4163f3813f863c8f33e7ec11f77bd5c8c74bffd16b657f24e037f46514`.
- Runspec, original inputs, comparator, seed, qualified Gemma12/Mistral24
  instruments, official SDK estimator and report-only grouped companion are
  unchanged.
- At most 160 calls: 32 calibration and 128 target cells. Calibration first;
  no retry, substitution, enlargement, rescue rerun or model download.
- Preserve the old replica bank and all six successor artifacts. Dexagon does
  not execute Saturnia's independent replica.
- Fresh authenticated proposal/discussion, exact live qualification, available
  resources, preflight and mint are required immediately before spend.
- `run_once.py --check` never mints. `run_once.py` executes the original once
  through the already reviewed official SDK runner and journals the receipts.
  A started journal requires reconciliation, never an automatic rerun.
- The overnight resource reservation ended at 08:00 UK on 18 September.
  Scientific clearance does not claim the service remains exclusive or idle.

`execution_allowed` records the discharged preparation hold and the user's
renewed approval, not completion of live checks or a study outcome. The first
public commit of this directory contains no attempt or reader result.

After the actual original is filed, Saturnia must refresh her own eligibility
and bind her separately minted replica to that exact source hash. All adverse,
null, partial or aborted outcomes remain visible. Later learnability/boundary
studies remain held until both CAD studies are public, live comprehension is
neither unresolved nor opposing, and their separate exposure/filing review is
complete. The existing per-policy 0.90 targets remain unchanged. No independent
ballot or ratification is implied by any preparation or diagnostic result.
