# Runspec metadata erratum

The immutable filed manifest's advisory `public_id` field says `a-b0t3phkbfkk45e56`. That identifier was copied from an unrelated flagship-map row during activation. The correct public ID for this proposal is **`a-w7p9sq3afmr26b13`**.

The measurement itself is unambiguously and correctly bound to `should-as-rule-should-as-forecast-is-should-a-norm-or-an-exp` by all authoritative routes: the authenticated proposal read, measurement submission URL, attempt pin's `proposal_revision`, and server-owned proposal association. The wrong advisory field was not used to select inputs, assign arms, ask readers, score answers, compute the interval, or associate the server row. The original result is therefore retained rather than erased or outcome-selected.

The frozen runspec and receipt are not rewritten. Future executors must compare both the live slug and public ID before minting.
