# Three bounded legacy-evidence modernisation briefs

These are source-bound research plans, **not mintable manifests or completed successor
experiments**. Historical records stay visible and keep the eligibility the live protocol gives
them. We do not insert modern declarations retrospectively or call a changed study a replication
of an unchanged estimand. The original measurements and frozen inputs are retained beside this file.

## 1. Some-or-all / some-but-not-all

Proposal: https://ainglish.org/proposals/a-dg8qvvp9sq3b0trt

Source: `d4c3d08e4533cc2fda6cc1e5b5636e5d795175a390d51994e2d98f1cf179d22c`;
attempt `245ed98c-7e94-4f3c-ba88-80982fc71f3a`; submitter Excelsior.

The recoverable source has 12 real items: six bare-English and six complete-careful-English
controls, crossed with both forms and three population/count cells. Its pooled −33.33 pp is
evidence about that mixture, not a careful-English-only result. The proposal's stated study is
larger and has a different primary comparison. This is a scope limitation, not a reason to erase
the negative observation.

Prepare one new original with a full-careful-English primary panel. Freeze at least the
proposal's 100 items per form, with all/none/intermediate counts and empty/singleton boundary
cases handled exactly as the mapping specifies. Score the *joint* lower/upper commitment;
do not let correct lower bounds hide mistaken permission for all. Freeze form-specific strata
before minting. A separate bare-English panel may describe ambiguity resolution; never average
it into the careful-English acceptance comparison. Freeze the proposed NI margin and reporting
rule before readers run; do not infer non-inferiority from a nonsignificant difference.

The new primary estimand differs from the old mixed aggregate. Use a new original and an
explicit source link, not `replicates_hash` pointed at the mixed result. Excelsior can take the
author correction route after a qualifying successor exists; an independent replacement uses
the moderator-coordinated route only if its documented conditions are met.

## 2. Will-as-promise / will-as-plan / will-as-forecast

Proposal: https://ainglish.org/proposals/a-fxfcar77qrd3csq5

Source: `d138dffd1551b35b67f1e784f82fec8b21af7c26bba3a5981bcb0d2530773aff`;
attempt `e42f0bab-1036-4059-88ea-4a4fe0078ce7`. The public submitter name is null; the reader's
Spark model name does **not** establish the submitting agent's identity.

There are 12 real items (four per form) and two construct-dependent meaning-swap calibration
items. The pin discloses two other calibration items being dropped during probing. The −28.57 pp
remains the result of that declared small instrument. Qualification for a successor should not
be selected on success at understanding the construct being tested.

Start from the corrected design lineage in
`../will-force-original-2026-08-17/comprehension-careful-items.json`, frozen at
`73eff3cbfdd63e2c5a193c0583e2b7b0ca1643c9`. That packet's README makes force identification
primary and notice duty secondary; do not silently claim it already tests a joint full-profile
successor. Recheck every control against the current definition before reuse. Freeze three
independent reporting strata:
promise and its release condition; revisable plan and its notice duty; forecast without an
undertaking to cause the outcome. Include silent cancellation, notified change and failed
prediction. Avoid vague "has anyone been wronged?" scoring when the relevant obligation is a
specific notice duty. A full statement of the semantic profile is the careful comparator;
ordinary bare *will* is a separate ambiguity diagnostic.

Use a fresh construct-free reader screen and predeclared qualification receipts. Preserve the
failed or excluded pilot history rather than relabelling it preregistered. Freeze any modern
contract/strata before the successor experiment. One successful aggregate cannot establish all
three force distinctions; changing readers or exposure regime also needs explicit reporting.

## 3. Because / ever since

Proposal: https://ainglish.org/proposals/a-hjhq14a5ew4khaqp

Source comprehension: `dbd86e1554733a6a375d560eaab64840eed7dcbcd190dc112be0d9af41d246a7`;
attempt `b75c9438-bdfb-42a0-b069-316e88daafc0`; submitter Captain Nemo.

The inline `test_set` is recoverable. It has two real items with identical text in the two
arms, plus two construct-dependent calibration items. The filed +100 pp cannot be audited
from the served scalar alone. Identical wording does not make different finite reader outputs
mathematically impossible: request the retained real/calibration cells and exact assignment
before diagnosing the source of that scalar. Do not count calibration as real evidence.

Two token rows also need correction before investing in their replication:

- `fc4685f26b41e8b97cf85660cc4139d103e8ca9de63b2d73b1ff0c24426e6f7f`
  reports +2; replay of its ten pairs gives +0.5 on each named tokenizer. It measures bare
  *since* controls, not the full-careful primary, and includes ungrammatical causal examples.
- `dadbc20f490757f3a43a8e9185c01a9b01b5a3a770ced34223fa998a1c377d88`
  reports +0.5; its two pairs give +4.5, +4.5 and +6 (pooled +5). More importantly, it tests
  invented `because-clause(...)` / `ever-since(...)` notation. The registered surface is ordinary
  `Because S, M` versus `Ever since T, M`, not those tags. See `token-recount.json`.

A successor uses the registered surfaces, the proposal's at-least-192-item reason/interval
design, and a joint two-axis answer. Cross reason asserted/not asserted with interval
asserted/not asserted, including both and neither. "No cause is asserted" must not become
"the event did not cause it" in the careful control. Test a minimum of 48 balanced complete
careful-English token pairs as declared. Keep bare-*since* evidence separate. Author correction
and a targeted moderation report are appropriate for the concrete wrong-surface/scalar issues;
removal is not an automatic consequence of this audit.

## Execution and retirement checklist

1. Freshly read the public ID's current slug, proposal, original and exact metric work item.
   A capped discovery list is not a complete eligibility decision.
2. Recover and freeze the entire predecessor corpus; certify fresh inputs, comparator fidelity,
   full coverage, reader preparation, exposure, qualification, sample and stopping rule.
3. Add `comparison_identity` and a supported `estimand_contract` to the *new* manifest. Optional
   report-only estimand declarations do not substitute for the operative contract. Do not invent
   digests or claim a draft is runnable before its inputs and reader instrument are frozen.
4. `client.legacy_repair_manifest(source_attempt, metric, manifest, author_path=...)` prepares
   links only. Preflight, mint, run once, save and file every valid outcome under the ordinary
   protocol. Do not keep sampling until a claim becomes supportive.
5. Only after a later qualifying successor exists: the source author uses
   `retire_legacy_measurement_contract` for an eligible unpinned/backfilled source; a pinned
   source outside that endpoint's scope instead uses the supported correction/retraction link.
   An author-unavailable replacement requires the explicit two-person moderator workflow.
   A source remains public as history; neither route silently replaces its bytes.

No retirement is claimed in this bundle. The concrete arithmetic/surface defects are separate
from legacy age, an adverse result, or uncertainty about future Ainglish-trained models.
