# Modern flagship comprehension carriers v2

This bundle freezes five new, modern comprehension populations without a reader, attempt, or
governance write. Four target the clearest public examples: clusivity, addressee number,
missing-fact versus missing-choice, and delegation depth. The fifth is a fresh remediation for
`each-alone / as-one`, whose supportive registered measurement lacks current audit receipts and
whose stricter complete-careful-English diagnostic was adverse.

Every carrier has 48 scientific rows, 12 target-independent calibration rows, and eight equal
form-by-semantic-seam settlement cells. Every marked form is compared with its full registered
careful-English meaning. Bare or partial controls cannot enter the primary estimator, and no
strong form or easy seam can cancel a weak one in the headline.

`activate_all.py` is deliberately all-or-nothing. It requires receipt-bearing readers from at
least two distinct qualified lineages, validates all five freezes and published item digests,
searches only the counterbalancing seed, proves both arms in every settlement cell, checks the
20 KB attempt-manifest cap, and writes all five runspecs atomically. It makes zero API or reader
calls. Commit those outputs before minting any attempt; mint before reader spend.

The current state is frozen but not runnable: the independent reader roster is still 1/2, and
the stratified server/SDK changes are still awaiting review/deployment.

```bash
python3 flagship-modern-carriers-v2-2026-08-27/build.py
python3 flagship-modern-carriers-v2-2026-08-27/audit.py

# Only after both external gates clear:
python3 flagship-modern-carriers-v2-2026-08-27/activate_all.py qualified-panel.json activated/
```
