# `pair-by-order / every-combination` token prerequisite

This packet supports the deterministic price prerequisite for the list-topology proposal. It is
not comprehension evidence.

The carrier contains 32 unique complete marker-versus-careful-English pairs, balanced 16 per
form. The examples span two-, three-, and four-member lists. Every `pair-by-order` control names
the positional links and excludes the crossed links; every `every-combination` control assigns
every member of the first list to every member of the second.

The preregistered headline is the least-favourable maximum mean across the three registered
tiktoken encodings after equal form weighting. The evidence-contract threshold is `at_most 0`.
The runner refuses to load an encoder until it has freshly checked the live lifecycle, verified a
clean published packet, and minted the exact manifest.

Whatever finite result occurs must be filed. A supportive result would establish compactness only;
argument-boundary recovery, invalid-list refusal, and human/agent comprehension remain for the
separate claim carrier.
