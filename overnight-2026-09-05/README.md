# Overnight evidence and progression work — 5 September 2026

New work after the previous batch, not a rewrite of its failed or adverse results.

## Instrument reliability comes first

The previous campaign's forced-guess controls omitted an honest "not determined"
option. One reader explained that information was absent and violated the frozen
zero-off-option rule. Its abort stands.

This batch tests one prospectively declared replacement control design, using the
same already-local readers and settings. Twelve development controls precede 24
untouched validation controls, per reader. No Ainglish target is present. Known
and unknown answers are separately balanced and separately checked. Maximum 144
calls, no retries or second configuration. Failure blocks target studies.

The SDK's planted-key gap is instrument sensitivity, not the semantic accuracy of
the information-absent arm. The latter has its own explicit unknown key and is
reported alongside the SDK receipt. No model training or human validation is
claimed. All generated files and source are retained with immutable commits.

## Planned scientific scope

Instruction persistence; date/zone clock interpretation; quantity updates;
shared/variable choices; probability/odds after prerequisite checks. Cold and
brief-reference conditions are separate prospective originals, not independent
replications or model retraining. Current token costs do not estimate future
tokenizers trained on Ainglish.

No new models, no model deletion, no staged language release.
