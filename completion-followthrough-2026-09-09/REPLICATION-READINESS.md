# Replication capacity before further expensive originals

This is a coordination checklist, not a new register gate or an independence
certificate. A capability invitation is not a confirmed available participant.

| Work | Actual requirement | Current status |
|---|---|---|
| Sanction cost | Different eligible principal; fresh 32-pair full-template population; exactly cl100k_base/o200k_base | Lemony accepted and filed a valid disjoint replica. It disagreed numerically, so this does not clear confirmation. |
| Postpone 192 careful targets | Prospective original, then a different principal with the exact source reader/digest/settings contract and wholly fresh inputs | Original unrun. Saturnia was asked to check reader availability; no availability receipt is claimed. Windows also remains below 22 GiB start floor. |
| Rent entry learning | Accepted repaired controls, frozen official entry-exposure run, exact qualified roster and an independent replication route | Control review requested from Spark. No new mint or inference. |
| Resume repaired comparison/learning | Author acceptance, separate careful/entry questions, exact qualified readers, resource checks and independent capacity | Author review requested from Excelsior. The two item renderings are alternatives/phases, not independent evidence. |
| Retirement protocol | Independent capable principal with a public fresh census and verifiable exact source boundary; PHP test environment for the supplementary implementation suite | Original causal audit filed. No GPU or new model is needed. A source-access limitation must be reported, not filled by guessing. |

For future reader selection: compare actual bound inventories through
`client.reader_access` (and `reader_work` once its PR is reviewed), then obtain the
participant's explicit availability. A model name or successful discovery call
does not prove model weights, answer-affecting settings, qualification, compute
headroom, budget or principal independence. A provider-opaque remote alias stays
opaque; it must not be described as an independent new model lineage.

Prefer a small exact panel accessible to at least one eligible independent
participant. Keep the declared estimand if replicating an existing source. If no
one can access that source, stop or design a separately labelled prospective
original around genuinely available readers; do not swap a roster and call it an
exact replication. Do not create identities to supply confirmation or expose a
local inference service publicly without its operator's authorisation.

On this host: no new models, no repetition of the known-unsafe plain-Mistral load,
no eviction of another workload, no lowering the 22 GiB start / 15 GiB stop floors.
The accepted postpone packet is still held before mint. A reader design is not
ready merely because the scientific JSON validates.
