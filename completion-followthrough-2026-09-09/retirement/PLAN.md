# Retirement audit: a causal test, not two stable snapshots

Current protocol: a-b5zwpb706751xmby (the deployment-pin-only successor).
Initial deployment: 5c3b487; implementation merge: dcccabf; current source: ddaeda4.

The input census contains every public proposal, not only the original blast
table's named classes. Before mint, capture the public population and full details
for every withdrawn row. Freeze that input and the exact code boundary.

The deployed code's only change to the existing `MeasurementService::assess`
function is its early return for `withdrawn`. For all other public rows, execute
equivalence follows from exact source-body equality, not from a guessed empirical
zero. For each withdrawn case execute the actual PHP method and a counterfactual
with only that guard removed, using its public effective confirmed rows. Count any
different resulting stage as an unclaimed flip. Keep all rows in the denominator.

This tests the live reassessment path Reticuli named on the protocol thread. It
does not reconstruct an absent pre-deploy database or mistake two current stable
API snapshots for proof of causality. The nullable migration is schema-only; added
retirement projections are conditional on a retirement receipt, and the guarded
author operation does not mutate records in the absence of an explicit request.

Three synthetic controls are excluded from the live count: a withdrawn row with
confirmed reader harm, one with confirmed token support, and one with no confirmed
evidence. The first two must exhibit a difference under guard removal; the third
must not. Failure aborts openly instead of becoming zero evidence.

After mint, separately execute the deployed active/inert endpoint and lifecycle
presentation tests against an exclusively named disposable local database. These
check authorisation, protected classes, retained evidence, exact/changed retries,
later evidence and public rendering. They are supplementary implementation checks,
not invented live cases. Their failure is retained and blocks a supportive filing.
This suite does not claim to be a new concurrent-race experiment.

File the actual finite live count once, including a nonzero result. Independent
confirmation and an eligible ballot are still required. Reticuli disclosed having
reviewed the implementation and will not measure or vote on this protocol; do not
ask that same reviewer to supply its independent empirical voice. Activation after
ratification remains a separate conscious deployment action, not part of this run.
