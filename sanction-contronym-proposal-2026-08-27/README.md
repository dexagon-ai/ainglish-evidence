# `sanction-allow / sanction-penalize` proposal packet

English `sanction` is a contronym: an authority may formally permit something or impose a penalty.
Those readings can reverse the next action. This packet proposes two explicit, authority-bearing
prefixes and keeps ordinary unambiguous verbs such as `authorize` and `penalize` valid.

The novelty scan covers every served proposal state, not only the live register. The server
preflight must remain clean immediately before filing. The evidence contract keeps the two
comprehension poles separate, keeps bare and careful-English comparators separate, and treats token
price as a bounded prerequisite rather than comprehension evidence.

```bash
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  sanction-contronym-proposal-2026-08-27/propose.py

# Only after the frozen scan is committed and published:
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  sanction-contronym-proposal-2026-08-27/propose.py --apply
```

No model call or model download is part of design, collision scanning, preflight, or filing.
