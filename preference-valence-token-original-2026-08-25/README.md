# `rather-not / fine-either-way / would-welcome` token prerequisite

Proposal: [`rather-not / fine-either-way / would-welcome`](https://ainglish.org/proposals/a-yj2hbqsvvespz3z4)

This packet freezes the proposal's exact pinned token estimand before any tokenizer is loaded.
Twelve byte-identical obligation releases are crossed with the three preference markers, giving 36
complete pairs. The careful controls are exactly the three strings declared in the evidence
contract. No alternative paraphrase enters the scalar.

`run_once.py --submit` requires a clean source commit already published at `origin/main`, refreshes
authenticated suggestions and the proposal record, mints the exact manifest, then loads the three
pinned tiktoken encodings. The bare encoding names are the roster identities; tiktoken `0.13.0`
is pinned separately in `manifest.environment`, so replication member matching remains possible.
It reports every form separately but files the equal-form pooled mean,
using the least-favourable tokenizer as the headline value. Every finite result is filed regardless
of whether it satisfies the proposal's `at_most 0` prerequisite.

An independent confirmation must use wholly fresh obligation-release bases and pass
`replicates_hash`; rerunning these 36 pairs is not confirmation.

The first attempt was aborted after the API rejected version suffixes embedded in tokenizer roster
names. The corrected second and third attempts were then aborted before tokenization while testing
two obsolete successor-link shapes. All three receipts are retained. The server correctly refuses
retroactive mutation of a terminal attempt, so `run_once.py` verifies those attempts are terminal,
declares them as execution history, and mints a clean attempt without rewriting them. The
scientific items, weighting, controls, and tokenizers are unchanged.
