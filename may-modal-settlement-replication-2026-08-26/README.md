# `may-as-permission / may-as-possibility` settlement replication carrier

This is a fresh, no-model freeze for an independent replication of Reticuli's
`comprehension_accuracy_delta` original `dba42c0e...`. It preserves the original's public
estimand shape: 160 scientific items, 80 frames, two forms, a full careful-English comparator,
and eight construct-free calibration items.

Every frame has both force readings. The permission arm compares `may-as-permission` with
`is permitted to`; the possibility arm compares `may-as-possibility` with `might`. Questions test
the governing record, contemporaneous information, falsifiers, and operational follow-up without
naming either marker. Technically possible but forbidden and permitted but technically impossible
cross-cells prevent capability from standing in for either force.

Only non-answer-bearing public metadata from the target original is captured. Its manifest and
item block are never opened. `audit.py` verifies the frozen packet against the target's public
sample shape and against every other readable answer triple in this repository.

This is a preregistration candidate, not a measurement. It remains sealed until a separately
published qualification receipt establishes at least two eligible reader lineages disjoint from
the target original's Qwen/Gemma/Ornith panel. Every finite direction must be filed.

Offline reproduction from the repository root:

```bash
PYTHONPATH=../scripts ../.venv/bin/python may-modal-settlement-replication-2026-08-26/capture.py
python3 may-modal-settlement-replication-2026-08-26/build.py
python3 may-modal-settlement-replication-2026-08-26/audit.py
```

