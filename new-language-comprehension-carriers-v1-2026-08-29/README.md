# Full comprehension carriers for the two new language proposals

This packet freezes the complete claim-bearing instruments promised by the `it(<ref>)` and
`none-of(<S>) / not-all-of(<S>)` proposals before either proposal receives a second or any reader
sees a scientific item.

For each proposal:

- the zero-shot governance carrier has **160 scientific rows** and 12 construct-free calibration
  rows;
- a wholly separate 160-row population supplies the definition-conditioned learnability
  diagnostic;
- Ainglish, balanced bare ambiguous English, and complete careful English remain separate arms;
- every form, hidden intent, antecedent position, interval boundary, question, and reader lineage
  must be reported separately before any pooled summary.

The definition-conditioned packets prepend one exact digest-bound reference card to every arm.
Their scientific populations share no messages or identifiers with the zero-shot packets. They
measure whether a frozen entry can be learned and applied; they cannot rescue adverse zero-shot
evidence.

## Activation boundary

These are public frozen carriers, not Ainglish measurements. A future runner must freshly re-read
the proposal and personalized suggestions, require the proposal to be measurable, validate two
independently qualified base-model lineages on one fresh construct-free holdout, mint the exact
manifest before the first scientific call, and file every finite admissible direction. No attempt
was minted and no model was called here.

```bash
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  new-language-comprehension-carriers-v1-2026-08-29/capture.py

/home/dexagon/codex/dexagon/.venv/bin/python \
  new-language-comprehension-carriers-v1-2026-08-29/build.py

/home/dexagon/codex/dexagon/.venv/bin/python \
  new-language-comprehension-carriers-v1-2026-08-29/audit.py
```

