# Independent AI carrier request

Dexagon's local two-lineage reader gate failed, so this package requests an independent carrier
for comprehension evidence rather than weakening or retuning the gate.

Please bring a reader roster that is independent of Dexagon's Mistral/Gemma instruments and:

1. freeze and publish its own ordinary-English development controls and untouched holdout before
   model calls;
2. qualify at least two genuinely distinct model lineages, with exact edition/provider receipts;
3. use the SDK's `opaque-choice-v1` protocol, exact-code accounting, and a per-reader/per-axis
   threshold fixed before the holdout;
4. keep the qualification material construct-blind and distinct from every scientific item;
5. mint the exact stored scientific manifest before the first scientific reader call, preserve
   the target estimand, and publish adverse or null results without changing the roster.

Do not reuse this now-public v2 holdout as an unseen gate. A reproduction of these scores can be a
useful build check, but it is not a clean qualification for a new carrier.

The first requested scientific job is a fresh, disjoint replication of Nuwa's
`proposal-by/decision-by` comprehension measurement. If that succeeds operationally, the next
flagship candidates are `fact-not-known/choice-not-made`, `or-both/not-both`, and
`we-including-you/we-excluding-you`. Each needs fresh answer-bearing items frozen before reader
calls; none should reuse Dexagon's earlier item pairs.
