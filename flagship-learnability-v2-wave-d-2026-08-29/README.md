# Flagship learnability v2 — wave D

No-spend definition-conditioned freeze for the four six-entry-cohort distinctions that were not
covered by waves A–C:

- `one-or-more(<role>) / exactly-one(<role>)`;
- `may-as-permission / may-as-possibility`;
- `some-or-all / some-but-not-all`;
- `whole(<S>) / part(<S>)`.

Every pole is a separate 48-item campaign with eight target-independent novel-marker
calibrations. The scientific message is byte-identical in both arms. A future released panel
harness prepends the digest-bound register entry only to the entry-loaded arm, so the estimand is
whether a reader can learn the registered meaning once and apply it to unseen cases.

The semantic cells preserve the principal-count, permission/possibility cross-force,
lower/upper quantifier-boundary, and population/subset distinctions. The role-cardinality items
are restricted to one explicitly bounded action instance; plural-event scope is not silently
answered. Every item also includes a non-claim or seam probe, so definition recall alone cannot
earn full credit by licensing extra meaning.

This is a research and qualification diagnostic, not the registered zero-shot claim carrier. A
good result cannot erase the adverse zero-shot evidence already filed for `some-or-all` or
`whole/part`, and it cannot repair the served evidence-contract mismatch on `may-as-*`.

No model call, attempt mint, or governance write is made here. The current local reader audit has
one qualified lineage while the carrier requires two, so the items remain sealed after public
freeze.

```bash
/home/dexagon/plugins/ainglish-openai-plugin/.venv/bin/python \
  flagship-learnability-v2-wave-d-2026-08-29/fetch_live_snapshot.py
python3 flagship-learnability-v2-wave-d-2026-08-29/build.py
python3 flagship-learnability-v2-wave-d-2026-08-29/audit.py
```
