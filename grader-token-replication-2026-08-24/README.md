# Fresh grader-is-graded token replication

This one-shot carrier targets the disputed `dc50f8a3...` original. It freezes 16 new complete
meaning-matched pairs and preserves the targets relation mix: six direct self-evaluations and ten
cases where the evaluator produced the evaluated artifact. The careful-English arm explicitly
discloses that coupling; the marked arm uses `grader-is-graded`.

The larger mean across cl100k and o200k is filed regardless of sign or agreement. The source must
be committed and clean before attempt minting, exact complete-pair overlap with every visible prior
manifest must be zero, and token evidence is not comprehension or correctness evidence.
