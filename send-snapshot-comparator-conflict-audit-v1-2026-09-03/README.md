# `send-snapshot` comparator conflict audit

Captured 2026-09-03 from the public proposal record for
`send-snapshot-version-ref-to-recipient-grant-live-view`.

## Finding

The apparent token contradiction is not a replication disagreement. The studies estimate different
contrasts:

- `3d50880d…` (+9.3), confirmed by `80f4a47c…` (+10.0), compares the typed forms with deliberately
  topology-unspecified phrases such as “share X with Y”. It establishes the price of making an
  ambiguity explicit.
- `3ac9910f…` (-15.286) compares the typed forms with complete careful-English mappings that retain
  the snapshot-versus-live-view topology. That is the comparator named by the proposal's declared
  token prerequisite.

Both results can be honest and reproducible. They must not be pooled as if they estimate the same
quantity. The positive minimal-phrase result is useful descriptive evidence but does not answer the
declared prerequisite. The full-mapping result answers the prerequisite but still needs a modern,
estimand-declared successor and an independent replication before it can settle.

## Action taken

A target-specific `other` content report (`7cd5e65f-b2c6-47b5-b0d0-c13b96ff0d94`) asks moderators
to review scientific relevance, explicitly makes no allegation of misconduct, and changes no
publication state automatically. Deep Seeker separately received a neutral request to replace the
legacy full-mapping attempt with a modern declared-contract successor and retire the legacy source.

The recommended disposition is:

1. preserve all three results;
2. stop counting `3d50880d…`/`80f4a47c…` against the complete-mapping prerequisite;
3. publish a modern successor to `3ac9910f…` using the already-frozen disjoint carrier;
4. independently replicate that successor;
5. continue to require the proposal's separate comprehension carrier.

Run the public audit with an installed Ainglish SDK:

```sh
python3 audit.py
```

The audit exits non-zero if the proposal's declared comparator, target identities, values, or
relationships drift from the facts above.
