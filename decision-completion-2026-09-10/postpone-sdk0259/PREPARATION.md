# Still unspent: prospective preparation reconciled to SDK 0.2.59

The original reviewed component is retained at
https://github.com/dexagon-ai/ainglish-evidence/blob/24ec25f/completion-measurements-2026-09-09/postpone-careful/STATUS.md
including its original 0.2.58 manifest and historical resource hold. No attempt or
reader call had occurred, so this is not a scientific rerun or an altered result.

The 10 September recheck found sufficient Windows headroom for the original
22 GiB start / 15 GiB in-run floor, no resident shared Ollama model, both exact
cached ctx4k artifacts, unchanged proposal/semantic acceptance, confirmed cost
and qualifications expiring on 16 September. These checks must be repeated at
execution; they are not a future resource guarantee or a reservation.

Current preparation produces a different commitment because the installed SDK
identifies the harness as 0.2.59 rather than 0.2.58. The complete manifest diff
contains **only `harness`**. The source change between those tags pins replication
targets and adds tests; this component is an original without a replication hash.
The qualification change accommodates plain roster IDs; this roster retains both
explicit precision labels. No new targets, golds, generation settings, calibration,
reader weights or outcomes were selected. The new official preflight is accepted
with `effect: none`.

Remaining planning gate: confirm an independent executor's actual access to the
same reader roster for a future fresh-input replication. An unanswered request is
not acceptance. Matching model-family names without matching artifacts/settings
are insufficient. No independent scientific voice is created by these local checks.

Once that gate clears, publish the complete execution freeze, refresh the exact
live contract and author discussion, mint once, run the official panel with the
original resource safeguards, and preserve every null/adverse outcome or abort.
This remains only the accepted 192-target careful-English component: not the bare
`table` advantage, broader validity/robustness programme or human validation. The
complete-answer constant baseline is 50%, not the random-option floor.
