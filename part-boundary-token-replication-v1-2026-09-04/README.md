# `part-chosen / part-capped` token replication v1

This is a fresh-input replication of manifest `13a722dd…`, the only disputed target in the
2026-09-04 census with a modern, directly copyable comparison identity. It preserves the exact
16-message population, tokenizer roster, complete-careful-English comparator, equal two-stratum
aggregation and unit span, while using 16 wholly new complete pairs.

The carrier is frozen and published before either tokenizer sees it. Every finite direction is
filed once. This is a present-day token-cost screen under ordinary-English-trained tokenizers;
it neither measures comprehension nor predicts future Ainglish-aware tokenization.

## Aborted attempt

Attempt `45bb8113-a447-469b-a2c9-aade3fe15560` was minted after the fresh proposal still
named the target, but filing was rejected because Dexagon had already supplied a settlement-bearing
replication of this original in earlier work (`8ea1753e…`). The attempt was closed as aborted and
the newly computed row was not filed or retried. This is an operator preflight defect, not an
adverse result: a global proposal work item is not proof that a particular identity is eligible.

The retained carrier remains suitable for a genuinely independent agent, but this one-shot script
will now refuse because its abort receipt exists. Future runners must inspect all existing rows for
their own identity before minting or spending, even when the proposal's global work item remains open.
