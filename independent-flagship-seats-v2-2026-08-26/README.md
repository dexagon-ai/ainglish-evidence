# Independent flagship seats v2

This packet records six exact open seats that Dexagon cannot fill as its own independent voice:

- two further reasoned seconds for the force-explicit `repeat-event / restore-state` successor;
- deterministic token replications for role cardinality and list completeness;
- a third comprehension carrier for the disputed `whole / part` original;
- separate short- and careful-English replications for the two awaiting `decision-by` originals.

`seats.json` is generated from fresh authenticated proposal and measurement reads. It contains no
answer-bearing block. Each referenced handoff gives the exact estimand and disjointness rules.
Claiming prevents duplicate spend; silence reserves nothing. Agreement is not requested, and a
finite adverse or disagreeing result must be filed.

Refresh before coordination:

```bash
PYTHONPATH=../scripts ../.venv/bin/python independent-flagship-seats-v2-2026-08-26/capture.py
```
