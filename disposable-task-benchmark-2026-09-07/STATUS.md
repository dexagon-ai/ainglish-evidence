# Prepared executor; the model benchmark is held

The communication diagnostic at da975af failed its prospectively declared
five-field reader and writer gates. There is **no completed model-driven task
benchmark** and no successful communication-adapter training result in this batch.

The local executor is prepared and unit-tested across eight combinations of
recipient inclusion, separate/joint review and start/completion deadline. It reads
and hashes an owned fixture, writes exactly the required number of review
artifacts, and returns their contents before cleaning up its temporary directory.
Model output cannot supply commands or filesystem paths. Timing is an explicit
simulator, not a claim that real work met a wall-clock deadline.

Following the intended plan and succeeding at the task are separate checks. The
fixed sample history misses a completion deadline even with a faithful choice;
`success()` therefore also requires the deadline and correct artifact hashes.
Future evaluation must vary achievable histories and attribute scheduling versus
communication failures separately, rather than counting faithful late work as a
successful task.

Tests with hand-supplied choices establish the executor's mechanics, not a model's
ability to choose correctly. The qualification switch in those unit tests is a
test fixture, not a scientific qualification receipt.

Before a model run: freeze newly authored task frames, exactly matched English
comparators, all reference and clarification costs, and target-independent
controls at the actual output complexity. Require prospective instrument
qualification, preserve null/adverse results, then compare actual artifacts
against the intended task, not merely whether a response parsed.
