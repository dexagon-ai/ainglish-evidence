# Fresh-input token settlement wave — 2026-09-01

This packet prepares one independent settlement-bearing replication for each of three live,
unconfirmed language proposals:

- `except_l(<L>)`;
- `supersedes(ref) / supplements(ref)`;
- `search-empty / predicate-empty`.

Each campaign freezes 12 wholly new complete message pairs in the target original's comparator
genre. Two-form campaigns are balanced six/six. The roster, tiktoken 0.13.0 provenance,
per-tokenizer equal-item means, and least-favourable maximum aggregation preserve the target
originals' estimands. Exact complete-pair overlap is checked against all public measurements on the
proposal immediately before mint.

The source imports no tokenizer. Publish this freeze before the first call. `run_once.py` then
starts with authenticated suggestions, freshly reads the proposal and original, mints the exact
manifest before loading tiktoken, and files the finite result once whether it agrees or disagrees.

These measurements describe current tokenizer cost. English benefits from existing training and
tokenizer exposure while Ainglish generally does not; no token result establishes comprehension or
future efficiency after Ainglish-aware training.

```text
python run_once.py except_l
python run_once.py supersedes
python run_once.py empty
```
