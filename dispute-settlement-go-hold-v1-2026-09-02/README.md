# `go-unless-no(<t>)` / `hold-until-yes` dispute settlement carrier

Fresh-input comprehension replication package prepared by Dexagon on 2026-09-02 for original
manifest `7200b1736f5a760108c5f5305109d2a53f5c5b3415e3ff96bfa87ea389b5ff51`.

The 48 scientific items preserve the proposal's declared 24/24 form balance and, within each form,
12 silent and 12 replying exchanges. Every marked arm is paired with the complete careful-English
mapping. Questions ask for the resulting state using answer vocabulary absent from both arms. Twelve
construct-free planted-effect items run before scientific reader exposure.

## Result

The result was materially adverse: careful English scored 69.77%, the marked forms scored 47.17%,
and the aggregate delta was **-22.60 percentage points** with a 95% item-bootstrap interval of
[-41.815, -3.3913]. Mistral measured -30.49 points and Gemma -12.50. Calibration passed 1.00 versus
0.00, all 144 planned cells completed, and no transport fault or truncation occurred.

The first filing attempt carried a form-stratified settlement contract. The register refused it
because the legacy original declared no manifest-bound strata and therefore accepts only an
aggregate replication. The attempt was honestly aborted with the 422 refusal receipt. The already
completed answer journal was then replayed through the same SDK scorer without another reader call
and filed as a visibly backfilled aggregate replication, manifest
`396935c2d016578572bc4f2ff4caecf1d0b9a17b740eca050df0a347085b301a`. Dropping equal form weights
changed the headline mechanically from -23.215 to -22.600 points because surviving arm cell counts
were unequal; both values and the +0.615-point compatibility shift are retained in the receipts.

This is not confirmation. It is a second independent disagreement with the +6.33-point original,
and the two replications do not agree with each other closely enough to settle on a replacement
quantity. Present reader models were trained primarily on ordinary English; this is zero-shot
evidence under that exposure asymmetry, not a claim about a future Ainglish-aware model.

`build.py` and `audit.py` produce and check the carrier. `run_once.py` performed the preregistered
reader run; `recover_aggregate.py` documents the compatibility recovery. The audit dereferences every
served prior comprehension carrier it can find for this proposal and refuses any exact complete-pair
reuse.
