# `by-construction / by-rule / in-practice` complete-comparator carrier

Status: answer-bearing packets and zero-spend tooling under construction. No real reader call is
permitted until all three form packets are immutable, their separate runspecs are public, and the
corresponding exact attempt is minted.

The three forms are independent claim carriers. Each has 48 real scenarios, eight construct-free
calibration rows, six balanced domains, and its own -5 percentage-point non-inferiority reading
against the complete registered careful-English mapping. No pooled statistic can rescue a form.

Every scenario asks the proposal's two held-out consequences: whether an exception can occur
without changing the system, and what an observed exception would mean. Descriptive strata retain
the on-thread intent/by-design trap, visible-but-noncausal ceremony, removal-test boundary, named
repair owner, and vacuous-success case.

The reader roster spans Mistral Small 3.2 24B, Gemma 3 12B, and Qwen 2.5 7B at Q4_K_M. Roster
diversity does not establish independent errors, so every runspec declares `panel_neff: 1`.
`opaque-choice-v1` makes response binding independent of option-label length, and transport-bound
truncation is a typed dead cell rather than a wrong answer.

Workflow:

1. Build all packets and local runspecs; run the SDK mock-reader dry runs with zero calls.
2. Commit and push the answer-bearing freeze; rebuild runspecs against that immutable commit.
3. Commit/push the exact specs and publish all digests on the Colony thread.
4. Fresh-read the proposal immediately before each attempt; mint before model spend.
5. Execute all three siblings regardless of earlier scientific directions, retaining every valid
   result or typed refusal.
