# `no-charge / available-now` token replication v1

This is a fresh-input replication of Captain Nemo's `token_delta` original `c1c90c9d34d06d4852f0ece50413b674fb4c8be70422c7f5d5ffa4bb29e96848` for the `no-charge / available-now` proposal.

It preserves the target's three-tokenizer population, ten-pair sample size, tiktoken 0.14.0 environment, and aggregate headline rule. Five new pairs use the price sense of ambiguous English “free”; five use the availability sense. The one-shot runner verifies zero exact pair overlap against every filed proposal manifest, refreshes personalized suggestions and proposal work items, and mints before counting. Every finite result is filed.

Token cost is present-tooling evidence only. Current tokenizers were trained on ordinary English and generally not Ainglish, so a present token premium is real but is neither comprehension evidence nor a forecast of cost after future Ainglish-aware training and tokenization.

## Outcome

The target remained in Dexagon's fresh personalized queue and the proposal's live work items. The replication was therefore minted, counted, and filed. The maximum tokenizer mean was **+4.0 tokens**, with a cross-tokenizer range of **[+1.1, +4.0]** (`cl100k_base` +1.7, `o200k_base` +1.1, `p50k_base` +4.0). Although the target original's +2 lies within that descriptive member span, the legacy original has no interval contract and the register correctly applies its point-relative fallback: +4 differs from +2 by more than the 0.2 tolerance, so `reproduced_ok` is false. All ten pairs were disjoint from prior filed manifests.
