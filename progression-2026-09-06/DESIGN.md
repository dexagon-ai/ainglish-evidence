# Because / ever since: a careful-English primary

Frozen before any target inference, 6 September 2026. This is a **new original**,
not a replication or a numerical correction of Nemo's legacy +100 result.
The live token prerequisite is satisfied; it will be rechecked immediately before mint.

Reuse the unspent 288-case source kit, with an opaque answer rendering that matches
the existing qualified reader settings. Nine domains, four equally weighted asserted
axis cells (reason, interval, both, neither), eight variants per domain-cell; question
and causal/temporal clause order vary. Exact joint recovery of the two asserted axes
is primary. Boolean ledgers and answer mappings are checked before inference.

The question is what the sentence **asserts**, not whether another relationship
might also hold in the world. Temporal wording does not assert lack of causation.
Nor does an interval beginning at an event prove that the underlying condition
could not have existed earlier. No earlier-onset or exclusivity gold is silently
inferred. The neither control uses identical meaningful text in both arms; the
runner retains its real allocation from the SDK cells, never guesses it from text.

Mistral Small 3.2 24B Q4 and Gemma 3 12B Q4 are already on disk. Reuse the exact
unexpired 5 September target-independent qualification receipts (64 output tokens,
temperature 0, seed 2026090581). Twelve new parcel-ownership controls per reader
use an honest unknown answer; they contain no target construct. Maximum 624 calls:
576 real and 48 controls. Mint and retain the attempt before calling any reader.
No model downloads, displacement of unrelated inference, adaptive reader selection,
target retries, rescoring or deletion. A failed gate ends the scientific campaign.

Publish the official SDK value, absolute arm accuracies, item-bootstrap interval,
each declared axis cell, each reader and each domain. The 36 domain-by-axis frames
repeat; add a separately labelled 2,000-draw cluster diagnostic using seed2026090597,
resampling frames within their axis cell. It never replaces the filed interval.
The four per-cell -5 pp comparisons are descriptive, not multiplicity-adjusted
proof of non-inferiority. Report adverse and null outcomes as readily as favourable.

This satisfies the primary sample-size target, **not the entire declared evidence
plan**. Bare-since advantage, earlier-onset compatibility, exclusive reasons, full
polarity/aspect balance, malformed-aspect rejection, date/duration controls and
robustness remain unmeasured here. No independent blinded human review is claimed.
Current English exposure advantages are real context; future-trained performance
remains unmeasured and does not erase present adverse observations.
