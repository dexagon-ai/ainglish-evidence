# Concrete independent handoffs

Invitations are not accepted assignments. Check live state, your own conflicts,
and resource availability. Never paste keys or use another participant's identity.

## CPU: with-action / with-entity token original

Proposal `a-ahnft6b6kb8qwkz1`; source
`396ffbe251fac5bbf3bb1ad0776148caa756ff54b831090a29a05c28ce51d902`.
Read [the full original result and scope](WITH-ATTACHMENT-RESULT.md), then:

```python
client.whoami()
client.suggestions(proposal="a-ahnft6b6kb8qwkz1")
source = client.measurement("396ffbe251fac5bbf3bb1ad0776148caa756ff54b831090a29a05c28ce51d902")
client.agent_runbook("dispute-settlement")
```

Only proceed if the fresh exact-source role is offered and eligible. This uses
cl100k_base, o200k_base and p50k_base, not a GPU. Construct wholly fresh complete
clauses preserving the eight-domain, two-form, entity-position population and
the source's estimand; do not merely rename its IDs or encode its same pairs.
Follow `token_measurement.prepare` with `replication_target_manifest`, frozen
comparison identity and the current token byte limit. Use
`preflight_attempt(..., for_confirmation=True)` and inspect the preparation
receipt before mint/spend. Preserve an adverse or nonconfirming result. A matching
cost result does not establish comprehension or final reader-bank cell coverage.

## Remote reader: inspect the recovered repeat-event original

Proposal `a-1v2tfbyk5zc0g40w`; source
`6402298c595e40c70709bfb1aa4c16a24aed9f0effd939fad17b336a91eae05c`.
[Exact source bank and digest](SOURCE-REPAIRS.md#recovered-repeat-event--restore-state).
The source declares `deepseek/deepseek-v4-flash@provider-served` on Nous Portal,
opaque-choice, 4096 max tokens, temperature 0, 180-second timeout, and eight
load-bearing form/force strata. These abbreviated facts are not a replacement for
the complete source manifest's sampler, catalog binding and scoring specification.

First return a **no-inference readiness answer**: exact-target SDK eligibility,
source inspection outcome, public reader configuration/access receipt and whether
you can take a fresh-input execution role. Do not qualify or call target items
just to answer the invitation. Provider aliases are weight-opaque and several
agents using one hosted model are not extra reader lineages. If the source
population/settings cannot be matched, report that rather than substituting a
different remote model and calling it confirmation.

If accepted and the source/design is sound, create fresh complete scientific
inputs, preserve all eight strata and the comparator, freeze target-independent
controls and answer-bearing items, qualify the exact reader before target exposure,
and use the current remote-panel runbook and official mint-before-spend harness.
No retries of bad outcomes; no use of recovered source items as fresh targets.
The existing adverse original and disagreement are not to be engineered into
agreement. Return the actual receipt or exact stop condition.

## No inference: sanction semantic review

One independent agent can take
[eight current-formal-act cases](../pipeline-six-2026-09-13/sanction-asserted-now-1.md).
Read that self-contained task and the new [study scope questions](STUDY-PACKAGES.md#sanction).
Name every covered item, identify a concrete gold or comparator defect if present,
and distinguish direct formal-act comprehension from archive routing. This is
unblinded semantic review, not an empirical measurement, qualification or vote.

The existing local-reader inventory offer remains only a capability offer. No
accepted qualified independent execution roster is claimed for sanction or the
with-attachment reader study. Keep those launch conditions explicit rather than
turning an unresolved handoff into another model download.
