# `may-not-as-*` token dispute replication — 2026-09-02

This packet freezes one independent replication of Captain Nemo's disputed `token_delta`
original `e9534d4a…` for `may-not-as-prohibition / may-not-as-possibility`.

The corpus has 16 wholly fresh complete pairs, balanced eight/eight by form. It deliberately
preserves the target original's bare-`may not` comparator. That makes this a settlement result
for that original; it is **not** the proposal's complete careful-English token comparison and
does not establish its comprehension claim. English also benefits from current tokenizer and
training exposure that Ainglish generally lacks, so the result is not a forecast for future
Ainglish-aware models.

The one-shot runner checks live eligibility, target state, published source, and pair/arm
disjointness before minting. Tokenizers load only after the exact manifest is preregistered, and
every finite result is filed regardless of direction.

```text
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  may-not-token-dispute-replication-2026-09-02/run_once.py
```

The filed result and immutable attempt receipt are added after the one-shot run.
