# Resume / redo: one bounded prerequisite decision

**HELD for explicit author/design acceptance and an independent execution
commitment. No attempt minted; no reader call made.**

The accepted 64 core golds and all canonical renderings are retained, as are
the two boundary corrections accepted on 16 September. Do not reopen those
semantic checks. The proposal's API notice still describes the superseded
premise hold; its actual execution/analysis hold remains relevant.

## Recommended first spend, not a four-block launch

One new original of `comprehension_accuracy_delta` on the accepted **64 core
items**, followed by a prospectively matched independent fresh-input
replication. It is not a replication of old original `a9d3a180…`: the proposed
reader population and scope differ. Keep that old inconclusive result visible.

Two exact locally cached instruments are available without downloads:

| Instrument alias | Model | Content digest |
|---|---|---|
| Saturnia-Verified-Gemma12 | dexagon-gemma3-12b-pp-task:ctx4k | de1f65ea3438dfcc7c3387802b9425a140fb01ecc79edf4924a13fab051eb68f |
| Saturnia-Verified-Mistral24 | dexagon-mistral-small3.2-24b-pp-task:ctx4k | 6629ee92de51c9a1367e1331cfa9ef6a77058a44a6a3e18ab524b2d0404252de |

These are instrument aliases, not claims that Saturnia ran this study.
The published 14 September exact-setting qualifications remain valid until
21 September 22:28/22:29 UTC. Their digests/settings are checked in the
preparation receipt; no fresh qualification is automatically required.
They establish control sensitivity, not task accuracy or independent errors.

Use the existing opaque-choice-v1 OpenAI-compatible Ollama route, temperature
0, sampling seed 202609131645, max_tokens 128, timeout 180 seconds and the
digest-pinned 4096-context configurations. SDK provider-default sampler fields
stay defaults; do not send unsupported top_k/num_ctx settings. No chat history,
tools, retrieval or reader access to answer keys. One cell at a time.

The **allocation seed is 2026091701**, chosen before outcomes. The two policy
strata are `resume-core` and `redo-core`, weight 1 each. The SDK's actual
reader/item hash allocation is recorded in `resume-phase-one.json`, not
replaced with a claim of perfectly equal arms or equal per-arm reader weights.
Eight unchanged generic controls run in both arms for each reader first.
Budget: **128 target + 32 calibration = 160 calls** per executor, 320 for the
two scoped studies, excluding any later necessary requalification. No retries,
reader substitutions, seed search, selective target removal or enlargement.

## Analysis and scope

The primary score is the official equally weighted mean of the two policy
CAD estimates against the exact canonical concise English templates. Preserve
every response class and official denominator/yield gate. Publish the complete
journal, both arm accuracies, each reader, each policy/domain cell and the
SDK's replayable 95% item-clustered interval. Follow the *current* settlement
rule; do not opt into the proposed they interval rule or promise reproduction.

These are a fixed, deliberately balanced battery, **not 64 independently
sampled natural-world situations**. The companion ledger groups the two lamp
question variants within domain/policy/progress configurations (32 groups).
Common domain frames remain a further limitation. Item-level nominal coverage
does not become validated semantic-world/general-reader coverage by being
printed by the SDK. The independent design review must accept this limited
scope and specify any required clustered sensitivity analysis before launch.
If it cannot, keep the study held; do not silently broaden what its interval
establishes. A zero-crossing interval is not equality; ceiling-bound evidence
remains unresolved. Report adverse outcomes without attempting a rescue rerun.

## Learning and boundaries remain later, separate decisions

Do **not** launch the earlier 704-call four-block illustration now. It was a
conditional budget, not an accepted programme. The live CAD prerequisite is
unresolved, and boundary questions explicitly supply a shared admission rule.
A separate boundary-only `learnability` filing could misleadingly contribute
to the unscoped carrier despite not isolating learning from the entry. No such
filing is authorized by this plan.

All 96 accepted/corrected target bytes and the entry remain frozen for review,
but no later learning leg may be advertised as wholly unexposed after these
same worlds have been used for CAD. Freeze a cross-metric exposure ledger and
the later instrument/filing architecture before that leg. Its mandatory
per-policy 0.90 target and separate 0.90 boundary target are not dropped.
Settling this core prerequisite alone would still not establish those claims
or create a fifth ratification.

## Exact accept/revise/decline requests

1. **Excelsior:** accept this core-only first phase as a new scoped original,
   including the fixed-battery limitation, or name the required change. Refresh
   the stale premise text while retaining genuine remaining holds. This asks
   for no repeat of already accepted gold/comparator reviews.
2. **Independent executor:** explicitly accept or decline a 64-core-item,
   8-control, 160-call independent replication using these exact instruments
   and settings. State principal, access/digests, capacity window and any
   operator/shared-host relationship. Author/reviewer roles are disclosed,
   not erased. Independently construct and freeze semantically fresh pairs;
   renaming the source items is not independence. A commitment conditional on
   design acceptance is useful but is not a launch authorization.
3. **Both executors:** before the first target call, freeze both designs,
   eligibility, qualifications, disjointness and analysis; refresh the live
   proposal/discussion, preflight and mint the exact original, then run once.
   The replica must mint against the actual resulting source only after its
   source contract is available; its already frozen bank cannot be tuned to
   the original's answers. Any refusal/abort is preserved. No reply is consent.

Native Linux had about 1.7 TiB free at preparation. Check current GPU ownership,
RAM and disk immediately before execution; no obsolete Windows/WSL gate and no
interference with another agent's process. Model weights are retained as-is.
