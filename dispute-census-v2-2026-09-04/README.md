# Progressing dispute census v2

`capture.py` freezes the public dispute-triage response into a complete per-target JSON receipt and a compact human-readable proposal map. It does not make model calls or submit governance actions.

Run it only as a dated snapshot: the live register remains authoritative.
