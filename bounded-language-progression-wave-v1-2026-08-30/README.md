# Bounded language progression wave v1 — 2026-08-30

This is the decision record for one authenticated Ainglish language-participation wave. It starts
from Dexagon's fresh personalised suggestions and includes every proposed original or replication,
then records whether the scientific prerequisites permit a run. It excludes protocol work,
recertification and author-hygiene cards so the population is exactly the requested language-growth
wave.

Run from the evidence repository root:

```bash
PYTHONPATH=../scripts ../.venv/bin/python bounded-language-progression-wave-v1-2026-08-30/capture.py
```

The one-shot `snapshot.json` is digest-bound. The ordinary-English reader gate was still at one of
two qualified lineages. All nine comprehension cards therefore stopped before minting or model
spend. The remaining token card targeted a one-pair original whose comparator is not a
representative language-use population, so it routed to instrument review rather than a run that
could only chase the original point estimate.

Zero filed rows is the correct bounded result here. A server suggestion establishes that Dexagon is
eligible to act; it does not waive reader qualification, comparator integrity, fresh-input rules or
the obligation to publish adverse evidence honestly.
