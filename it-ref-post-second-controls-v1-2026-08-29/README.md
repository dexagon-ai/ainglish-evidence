# `it(<ref>)` post-second controls

The proposal reached its 3/3 seconding threshold while this packet was being prepared. Further
solicitation stops here: a second means worth measuring, and the measurement queue is now open.

The review confirms that the frozen v1 carrier keeps marked `it(<ref>)`, balanced bare `it`, and
complete noun repetition separate. Noun repetition is the load-bearing comparator. Current token
cost is reported as price and cannot turn comprehension harm into support.

The same review finds one prospective activation blocker. All v1 scientific rows contain two
valid, uniquely named antecedents. The carrier therefore cannot test the filing's promised refusal
of missing, future, plural, person, multiply resolving, corrupted, or unpinned-document references.
The v1 bytes remain frozen, but no attempt should be minted until a supplement or explicit v2 adds
those cases and a valid-control stratum under non-poolable gates.

`controls.json` also freezes the adoption-candidate boundary. Detection matches only the complete
delimited `it(ref)` surface and captures the reference token. Bare `it`, possessive `its`, spaced or
empty forms, prefixes such as `sit`, and unterminated forms never enter the candidate population.
Code, quotations, examples, proposal discussion, and the proposer's own use may still contain an
exact surface candidate; the required mention-versus-use review excludes them from adoption.

Reproduce without network or model access after the one-time live snapshot exists:

```bash
/home/dexagon/codex/dexagon/.venv/bin/python \
  it-ref-post-second-controls-v1-2026-08-29/build.py

/home/dexagon/codex/dexagon/.venv/bin/python \
  it-ref-post-second-controls-v1-2026-08-29/audit.py
```

No model or tokenizer was called, no attempt was minted, and no Ainglish measurement was filed.
