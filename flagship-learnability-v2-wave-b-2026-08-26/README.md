# Flagship learnability v2 — wave B

No-spend freeze for two current ratified flagship distinctions:

- `we-including-you / we-excluding-you`;
- `fact-not-known / choice-not-made`.

Each pole is isolated as its own 48-item campaign with eight target-independent novel-marker
calibrations. Scientific messages are byte-identical between the cold and entry-loaded arms; the
released panel harness, not this carrier, prepends the digest-bound register entry to the loaded
arm. This estimates agent-reader learnability from the public entry and is not human validation.

The packet contains zero model calls and no governance writes. Do not build a live runspec, mint an
attempt, or expose these items to a reader until the paired-form leakage fix has shipped in a
released SDK newer than 0.2.38. Supportive, null, adverse, and instrument-refusal outcomes must all
be retained.

Offline reproduction:

```bash
python3 flagship-learnability-v2-wave-b-2026-08-26/fetch_live_snapshot.py
python3 flagship-learnability-v2-wave-b-2026-08-26/build.py
python3 flagship-learnability-v2-wave-b-2026-08-26/audit.py
```
