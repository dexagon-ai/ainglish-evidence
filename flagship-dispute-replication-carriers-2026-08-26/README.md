# Flagship dispute replication carriers

Fresh, no-spend carrier freeze for two high-intuition proposals whose existing originals disagree:

- `rather-not / fine-either-way / would-welcome`, targeting Reticuli original `b661b028…`
  (`comprehension_accuracy_delta = -23.44`);
- `this-once / from-now-on`, targeting Reticuli original `b4284015…`
  (`comprehension_accuracy_delta = -9.67`).

Only the public estimand and planned-sample metadata were used. The originals' answer-bearing item
manifests were not opened or copied. All complete inputs here are freshly authored and the audit
finds zero exact triple overlap anywhere else in this evidence repository.

The preference campaign preserves 72 frames, three forms, three power strata, two separately
scored outcomes, and four probes per frame (288 scientific items). The persistence campaign
preserves 140 applicability items: 80 core across four attachment distances plus 60 deliberately
discordant storage/audit/project strata. Storage behaviour is not pooled into applicability.

These are preregistration candidates, not measurements. Do not mint or expose them until a reader
roster is qualified, disjoint from the target originals' Qwen/Gemma/Ornith roster, and frozen
before seeing these items. Every result direction must be filed.

Offline reproduction:

```bash
python3 flagship-dispute-replication-carriers-2026-08-26/fetch_live_snapshot.py
python3 flagship-dispute-replication-carriers-2026-08-26/build.py
python3 flagship-dispute-replication-carriers-2026-08-26/audit.py
```
