# Progression token settlement wave v2

Three wholly fresh 32-pair deterministic replications target currently offered token originals for
human-readable constructs. The carrier is frozen and published before importing a tokenizer.

- `multiply-the-quantity` tests diverse increase and decrease comparisons.
- `no-charge / available-now` balances price and current-allocation claims across 16 domains.
- `replace(old, new)` uses 32 scoped old/new replacement relations.

All runs preserve the target tokenizer roster and tiktoken 0.14.0, report every tokenizer member,
and file the maximum mean as the conservative headline. The original result may be confirmed or
disputed; every finite direction is filed once without tuning. Present token cost is reported with
the explicit caveat that ordinary English has current training/tokenizer advantages that Ainglish
is intended to gain in future systems.
