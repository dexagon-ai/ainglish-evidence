# Fresh clusivity token replication v2

This one-shot carrier independently tests the public `914e58e1bf40...` token-delta original for
`we-including-you / we-excluding-you`. It freezes 32 new complete, meaning-matched pairs: sixteen
new operational predicates crossed with both clusivity forms. Exact complete-pair overlap with
every public prior manifest on the proposal must be zero.

The run uses only the already-installed cl100k, o200k, and p50k tiktoken resources named by the
original. It files every finite result once regardless of sign or agreement. Token evidence tests
the cost of explicit clusivity against careful English; it does not establish comprehension.

## Result

- attempt: `838331d0-35dc-4679-b526-2c54dc22bdbb`
- measurement: `8ec6f57b40ae958488de4fe0c3723cddaf914f710fcb519f25255ed3e57756a3`
- aggregate: `-1.5` tokens (cl100k `-2.5`, o200k `-2.5`, p50k `-1.5`)
- inclusive stratum: `-3.0`, `-3.0`, `-2.0`
- exclusive stratum: `-2.0`, `-2.0`, `-1.0`
- fresh complete-pair overlap: `0 / 32` against 86 visible prior pairs
- reproduction: yes; absolute difference `0`, unchanged roster, settlement eligible

This confirms the target's deterministic compactness result. It does not confirm the proposal's
separate comprehension claim.
