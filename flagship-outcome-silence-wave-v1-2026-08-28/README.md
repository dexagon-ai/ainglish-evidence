# Flagship outcome and silence wave v1

This bundle prepares the two next flagship comprehension carriers that are genuinely ready for
reader work, and refuses to spend readers on two superficially nearby rows whose live evidence says
repair or disagreement comes first.

- `test-run(<T>) / test-passed(<T>)`: 96 scientific rows, split into twelve equal form by semantic
  seam strata. Terminal execution, declared acceptance, constituent-subcheck execution, broader
  fitness, current fitness, and independent verification remain separately load-bearing. A
  separate validity audit covers missing criteria, ambiguous run identity, scheduled-only work,
  and aborted work.
- `go-unless-no(<t>) / hold-until-yes`: 96 scientific rows, split into twelve equal form by
  addressee-behaviour strata. Silence, an explicit decline, a deferral, a question, an explicit
  approval, and a late objection are all tested against the writer's inbox clock. Separate
  diagnostics retain the no-authority and no-receipt boundaries.
- `some-or-all / some-but-not-all`: no new same-principal reader spend. The live row already has
  disputed comprehension evidence; another convenient run would dilute rather than resolve it.
- `may-as-permission / may-as-possibility`: no reader spend. Its live token prerequisite is
  confirmed adverse, so author repair or a genuinely independent challenge must precede the
  expensive claim carrier.

Every primary row compares the marker with its complete current careful-English meaning. Bare
English appears only in labelled descriptive sidecars and cannot satisfy the governance metric.
All answer-bearing bytes are frozen before any reader, model, tokenizer, attempt, or measurement.

The publication is intentionally two-step. First freeze and publish the item bytes. Then rebuild
the URL-bearing templates against that immutable commit and run the strict audit.

```bash
PYTHONPATH=/path/to/ainglish/src python3 flagship-outcome-silence-wave-v1-2026-08-28/capture.py
python3 flagship-outcome-silence-wave-v1-2026-08-28/build.py
python3 flagship-outcome-silence-wave-v1-2026-08-28/audit.py --allow-placeholder

# After committing and pushing the frozen item bytes:
python3 flagship-outcome-silence-wave-v1-2026-08-28/build.py --item-commit <40-hex-commit>
python3 flagship-outcome-silence-wave-v1-2026-08-28/audit.py
(cd flagship-outcome-silence-wave-v1-2026-08-28 && sha256sum -c SHA256SUMS)
```

The templates remain non-runnable until an exact panel with two independently qualified base-model
lineages is bound. Activation must be committed before minting, and minting must precede the first
scientific or calibration reader call.
