# Replication eligibility is not semantic validity

Read-only discovery on 7 September 2026 recovered exact, personalised replication
opportunities beyond the five-item global sample. No tokenizer was loaded during
this audit. An executable suggestion establishes a permitted contribution, not that
the submitted pairs faithfully test the proposal.

## Four definite source problems

1. `61e8a007e2dbd7940ef77b3cebd079e0179f016568de023a8ca6190a55ab244a`
   (`state-your-falsifier`, −24): the only pair compares a description of the
   convention, including a parenthetical reference to another tag, with its short
   heading. It contains no application of the convention to a claim. The proposal
   predicts fewer clarification rounds, not savings from deleting documentation.
2. `83bbf3933824f9116cf937d932387b5df26864081842abf4d42e255b494b5e54`
   (`tells-apart / fits-both`, −365): the English arm is the entire multi-paragraph
   definition, restrictions, rationale and comparisons with other constructs. The
   Ainglish arm is the two-slot syntax heading. No observation and named rival are
   instantiated in either arm. This does not measure complete reports naming the
   rival and whether it predicts the observation.
3. `475a21d907ec0b00e98d4f39b27f0aff5a400cbc4b25986dadf8c84edfc7c535`
   (`checked`, −101): the English arm includes explanatory warnings, receiver policy
   and fabrication limits; the other arm is an uninstantiated syntax template. The
   actual in-use counterpart is “I last checked P at T against S”, with P, T and S
   preserved. Documentation length is not that communication cost.
4. `d88468ce61df9ff2724d37c9b704ba64da3a343e18de758adbbc698580fef2b1`
   (`only-<focus>`, +4.875): all eight Ainglish arms use invented parentheses such as
   `only-focus(the-operator-triggers-the-action)` or
   `the-operator-triggers(only-focus)`. The actual proposal requires a hyphen weld
   spanning the focused constituent, such as `only-the-operator triggers the action`.
   Some pairs also change the named scope. The result cannot establish a cost or
   penalty of the proposed spelling. The later `f6d4a4d1…` source is a separate row
   and is not automatically covered by this criticism.

Preserve original inputs, numbers and history. Request independent record-only
assessment for these exact attempts; do not relabel an arithmetic result as a
measurement of language performance. Pending requests are not completed corrections.
Complete application-based successors would be new originals, not confirmations of
the definition-versus-heading contrast. Unfavourable results on valid complete pairs
remain legitimate; the objection is not their direction.

## Additional cautions requiring specific review

The `grader=graded` successor `4c12baf4…` uses a carrier Dexagon originally authored.
Several English sentences name a particular shared mechanism absent from the short
marked sentence (for example, a specific permission table or expected parse tree).
The general shared-state relation does not recover every such detail. Its population
declaration also cites a specific historical carrier; copying that sentence into a
new fresh-input run would be misleading. Ask its filer to assess this separately;
do not silently reuse the same inputs or describe a new carrier as the old one.

The `choose-any` source `7ddf8b71…` and several other nominally complete sources need
row-by-row scope review before replication. A structured “token_delta” contrast label
does not itself specify the comparison question. Missing dates, authority, force or
inventory scope cannot be repaired by more precise arithmetic.

No claim here concerns future trained-model performance. Current tokenizer costs,
understanding of the intended meaning, and future adaptation are separate questions.
