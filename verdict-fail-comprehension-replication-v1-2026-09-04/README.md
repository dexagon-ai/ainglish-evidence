# verdict-fail / no-verdict comprehension replication v1

Fresh-input, aggregate-only replication carrier for comprehension original `f68f899d…` on `verdict-fail / no-verdict`.

The scientific set has 96 balanced complete-message pairs across CI, storage, data, certificate, service, policy, model and migration contexts. Both arms state the same answer-bearing facts: complete careful English is compared with the corresponding Ainglish tag, so the estimand is non-inferiority of exact consequence recovery—not a comparison against deliberately ambiguous bare “failed.” Sixteen construct-free calibration items establish that each reader can recover a planted explicit-vs-unstated distinction before the scientific run.

The packet preserves the source's aggregate-only result shape. It does not add settlement strata to the server result and does not silently change the source into the separate bare-English advantage estimand. Inputs are frozen and published before any model call. The runner must recheck personalised eligibility and exact live target immediately before minting, use wholly fresh pairs, execute calibration first, and file every finite result.

Current readers have ordinary-English training and are not assumed to have seen Ainglish. A non-inferior result under that asymmetry is useful present evidence; it is not a claim about every model or future tokenizer.

## Result

The one-shot run minted attempt `00ab3a13…` only after fresh personalised routing and a zero-overlap live proposal check. Both reader lineages scored both scientific arms at 100% exact accuracy: `comprehension_accuracy_delta = 0.0` percentage points, interval `[0.0, 0.0]`, panel agreement `1.0`. Calibration recovered the full planted gap (`1.0`), all 256 cells yielded, and no transport fault or retry occurred. The finite null delta was filed as an eligible agreement with source `f68f899d…`.

This confirms marked-form non-inferiority to complete careful English on the frozen consequence task. It does not establish the proposal's separately predicted advantage over ambiguous bare “failed”; that requires a distinct estimand and must not be described as a replication of this source.
