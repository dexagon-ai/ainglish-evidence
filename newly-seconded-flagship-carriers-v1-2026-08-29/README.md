# Newly seconded flagship carriers v1

Status: **inputs frozen; both token originals filed; reader activation closed**.

| Proposal | Least-favourable balanced token delta | Registered prerequisite |
|---|---:|---:|
| `mean-of / median-of` | −14.5 | ≤ 0, satisfied |
| `removed-from / erased-from` | −20.125 | ≤ 0, satisfied |

Both are current-tokenizer price results only. They do not establish comprehension and should not
be read as a claim that today's models have learned either new surface.

This bundle turns two newly seconded, human-readable distinctions into answer-bearing evidence
populations while preserving their registered estimands.

- `mean-of / median-of`: 160 rows, 80 per form. Eighty immutable finite datasets are matched
  across both statistics. The population includes skew, symmetry, negative values, outliers,
  even medians not present in the data, changed windows/exclusions, sample boundaries, and
  invalid weighted/rolling/categorical uses.
- `removed-from / erased-from`: 160 rows, 80 per form. Eighty persistence scenarios cover query
  surfaces, principal and region boundaries, stale replicas, omitted object versions/WAL/delayed
  replicas, cryptographic-erasure models, later backups, tombstone/boundary issues, and false legal
  or future-state inferences.

Each row contains separate bare, complete-careful, and short-practical comparator arms. Forms and
comparator classes have separate templates and must never be pooled. The frozen token prerequisite
uses 32 same-semantic-cell pairs per proposal, balanced 16 per form, against complete careful
English. Its result is only a present-price prerequisite: current tokenizers may have seen English
but not Ainglish during training, and no token count establishes comprehension.

No reader may see a scientific item until an immutable panel of at least two independently
qualified base-model lineages is committed and the exact attempt is minted. The local installed
roster currently has only one qualifying lineage, so this bundle deliberately performs no reader
call and downloads no model.

Freeze and audit without model/tokenizer calls:

```bash
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  newly-seconded-flagship-carriers-v1-2026-08-29/capture.py
python3 newly-seconded-flagship-carriers-v1-2026-08-29/build.py
python3 newly-seconded-flagship-carriers-v1-2026-08-29/audit.py
```

Only after the frozen packet and runner are committed and public, run one token target at a time:

```bash
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  newly-seconded-flagship-carriers-v1-2026-08-29/run_token_once.py average
```
