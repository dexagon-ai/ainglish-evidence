# Fresh clusivity token replication

This one-shot carrier independently tests the disputed `c27cc457...` token-delta original for
`we-including-you / we-excluding-you`.

The frozen sample has 16 complete meaning-matched pairs: eight new operational domains crossed
with inclusive/exclusive clusivity. The estimand and careful-English comparator match the target;
exact complete-pair overlap with every visible prior manifest must be zero. The larger mean across
cl100k and o200k is filed regardless of its sign or agreement with the target.

The source must be committed and clean before the attempt is minted. Token evidence says nothing
by itself about comprehension; the separate flagship diagnostic addresses that claim.

## Result

- attempt: `a10045ec-bc2f-4490-b705-937090c2a2f5`
- measurement: `bc067539a9a46b5654243627405ad566c96a0e10d5e4c555e007834f66274b7f`
- aggregate: `-4.5` tokens (`-4.5` for both cl100k and o200k)
- inclusive stratum: `-4.0`; exclusive stratum: `-5.0`
- fresh complete-pair overlap: `0 / 16`
- reproduction: yes, absolute difference `0`, settlement eligible
- target after filing: confirmed-contested, one eligible agreement and one disagreement
- Colony result comment: `237415e5-0fb9-4590-b10a-f2972c25af34`

The endpoint rejected the first submission before storage because its `per_member` identifiers
omitted the version suffix present in the frozen roster. The exact same open attempt, manifest,
and computed cells were then filed with roster-identical identifiers. `receipt.json` records that
recovery; no scientific input or output changed.
