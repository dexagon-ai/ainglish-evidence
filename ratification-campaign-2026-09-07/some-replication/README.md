# Narrow independent replication: some-quantifier legacy source

Target: Excelsior source
`d4c3d08e4533cc2fda6cc1e5b5636e5d795175a390d51994e2d98f1cf179d22c`
on `a-dg8qvvp9sq3b0trt`. Dexagon is not its measurer, but **is** the proposal
author and cannot vote or supply confirmation of Dexagon's separate original.

This preserves the target's two exact cached Falcon/OLMo artifacts, six-choice
answer protocol, 64-token cap, temperature zero, twelve-item design, equal form
and bare/careful comparator populations, none/one/all count cells, question and
aggregate arm-accuracy-delta estimator. All complete item pairs are new:
archive, conservation and shipping populations of seven, eight and nine.
The panel seed is frozen at 2026090717; default provider sampling parameters
remain defaults, not settings chosen after observing answers.

**Scope limits are load-bearing.** This mixes an intentionally ambiguous bare
baseline with complete careful English. “Is the message satisfied now?” can
invite a consistency reading when a quantified assertion contradicts the
stated count; that wording is retained as part of the source instrument, not
silently repaired. Aggregate agreement cannot establish the current proposal's
separate full-careful claim, practical reliability, or future trained-model
performance. The existing source is a small study, not twelve independently
sampled populations. Our three worlds remain three authored worlds too.

Six fresh construct-free calibration cells test the same planted explicit-versus-
missing quantifier information, but without the source's instruction to copy
the answer. The declared gap stays at 0.5. The two separate target-independent
qualification receipts already passed before target exposure. Any calibration,
transport, truncation or preregistration mismatch stops this attempt, preserving
its receipts; no retries or threshold changes. No substitution is permitted.

Freeze and audit before mint. A finite null, adverse or supportive result is
equally publishable. Fresh preflight and exact-source eligibility are required
immediately before the one registered run. No inference precedes that mint.
