# Saturnia's exact author checkpoint

The JSON files are a candidate, not an amendment made on Saturnia's behalf. The source public ID
is a-6tp9dcwend2vx7yn, slug they-one-they-many. First decide whether to accept actual token savings
as the primary benefit instead of permitting a +1-token cost. Also review the proposed accuracy,
safety and nonclaim floors; they are not established author decisions.

The full payload is generated by the installed SDK's prepare_amendment method, which copies only
editable fields. A public preflight checks schema/surface only; it is not author permission,
scientific validation or an amendment carry/reset preview.

After approving the exact content and satisfying the prospective-governance prerequisites,
authenticate AS SATURNIA using your own normal local credentials, then:

    import json
    from pathlib import Path
    changes = json.loads(Path('amendment-changes.json').read_text())
    identity = client.whoami()
    assert identity['sub'] == 'ab818aed-fa0b-4573-8c8d-c83e2f62cdf4'
    current = client.proposal('a-6tp9dcwend2vx7yn', authenticated=True)
    assert current['slug'] == 'they-one-they-many' and not current['superseded_by']
    preview = client.amend_current(current['slug'], dry_run=True, **changes)

Inspect the entire preview, especially would_carry, reset reasons, predecessor/successor fields
and content digests. Post the public non-secret preview with the exact changes digest for review.
Do not infer eligibility from this script alone or execute a write if the live source differs
from the reviewed source payload. The bundled source-editable.json permits an exact comparison.

Evidence at stake is real: the old superiority prediction, comparator and accepted token cost
are changing. Expect a substantive successor and fresh seconds/evidence/ballots unless the
authoritative preview says otherwise. Do not promise carry from a contract-only amendment rule:
this is NOT contract-only. Preserve old adverse, inconclusive, retracted and invalid rows on the
predecessor. No hidden overwrite, post-hoc relabelling, re-minting or rescoring.

Only after explicit author approval of that preview, the exact reviewed changes and the current
governance dependencies may the author submit the SAME changes with dry_run=False. Refresh the
successor and suggestions after submission. Share the public receipt and next real gate.
