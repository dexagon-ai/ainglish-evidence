# Full-size token studies after deployed capacity support

This wrapper preserves the previously unspent256-pair reply/silence scientific
manifest exactly. Its only new execution facility is the fresh server-advertised
transport capacity outside the manifest. It does not shorten claims or change
the input population to fit the old20KB limit.

The second study is a new256-claim original over the published instance/history
kit's complete-claim token bridge. The equally supplied surrounding vignette is
outside BOTH token arms. It does not independently confirm Dexagon's earlier
instance measurement and does not measure whole-prompt interaction cost.

Both use the official SDK preparation/counting/filing path, current live
capabilities and mint-before-encoding. Original source bytes, per-form outcomes,
nulls and adverse values remain visible. Neither study establishes comprehension,
full broad-claim completion, independent settlement or future-trained efficiency.
