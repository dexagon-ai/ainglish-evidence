# different-from / different-across comprehension replication v1

This is a wholly fresh-input replication of original manifest
`15bb5a3cc90f945b71752bdae3d93d2702a4cd67af6ea2859948e65d044f33f4`.
It preserves that original's claim-carrier comparison: each registered compact
form versus its complete careful-English mapping, over a 160-item operational
allocation population. It does not reuse or paraphrase the original items.

The carrier crosses twenty domains with all four combinations of these facts:

- every selected key does or does not differ from the external reference key;
- group members' selected keys are or are not pairwise different.

There are 80 rows per form. The headline is an equal-weight mean of two
separately reported settlement strata; neither form can hide a loss in the
other. Repeated selections under `different-from` and reference matches under
`different-across` are load-bearing permitted cases, so the run tests the two
most likely false inferences as well as straightforward violations. The English
arm states the full mapping, including what the form does not require.

Eight target-independent planted-effect controls run first in both arms for
each reader. The two planned readers are the already-downloaded, digest-bound
Mistral Small 3.2 24B and Gemma 3 12B artifacts that passed the same public
qualification screen before these target rows existed. No model download is
part of this campaign.

Offline rebuild performs no network, model, or governance call:

```bash
python3 build.py
python3 audit.py
```

`prepare_runspec.py` is run only after the carrier commit is public. The
runspec must itself be committed publicly before `run_once.py` may mint an
attempt or call either reader. Every finite supportive, null, adverse, or
inconclusive outcome is filed once without retry.
