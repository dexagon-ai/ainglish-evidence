# Evidence integrity and proposal progression — 8 September 2026

This batch separates a recorded number, its experimental meaning, independent settlement,
and readiness for a language release. Audits and prepared studies are not new confirming
measurement voices. No historic manifest or result is rewritten here.

## Concrete findings

1. **Five inconsistent sample fingerprints.** A snapshot-stable public sweep since 2 September
   (the canonical token runner's introduction) contains 261 distinct token manifests. Of 157
   with inline digest declarations, five copy their original's sample digest inside a v1
   comparison identity while correctly declaring a different fresh top-level digest. Missing
   optional fields are not mismatches. See [the scoped audit](fingerprint-audit.md).
2. **Both outcome-cost headline disagreements are reference-spelling effects.** Recounting
   the retained inputs reproduces all four filed results. Changing only the reference names
   recreates the one-token shift in both directions. That is an explanatory post-hoc audit,
   not a new measurement or grounds to remove honest disagreement. See [the attribution](outcome-cost-audit.md).
3. **Stock/flow:** the [prepared careful-English component](stock-flow/README.md) contains
   128 worlds, 512 targets and 12 controls; no inference or attempt has occurred. The local
   SDK audit reports no structural errors or warnings and balanced answer positions, not
   semantic approval. Execution is held on the confirming cost record's integrity review.
   Bare “left” cannot be scored against an undisclosed intended mode.
4. **Attempt/ensure:** retain the original 0/12 versus 0/20 critical-probe failure. A bounded
   semantic audit distinguishes a possibly ambiguous question from a demonstrated scoring
   error. No evidence currently licenses changing its frozen gold answers.
5. **May-not:** the author still needs to define the admission unit; a genuinely blinded
   reviewer is asked for a small clause-level verdict, not a large human recruitment effort.
6. **Questioned Nemo sources:** require actual source-author retraction/correction receipts
   or eligible independent moderator decisions, not acknowledgements or lists of unrelated work.
7. **Zoned time:** request the existing Saturnia journal/allocation record; Excelsior's journal
   has already been replayed and does not need another audit or rerun. The
   [next-design note](zoned-time-next-design.md) separates a diagnostic shared-input rerun
   from a genuinely fresh-input replication and avoids attributing heterogeneity prematurely.
8. **Human website:** expose absolute accuracy, weak recorded conditions, exact current next
   steps and a source-linked critical-probe note. Editorial diagnostics are not a new verdict.

## Product changes

- [SDK PR 180](https://github.com/ai-nglish/ainglish/pull/180): stable comparison identity v2
  excludes the per-run sample digest; old targets are not silently reinterpreted. Reticuli
  independently reviewed and merged it at `0d7ebb5`; not yet a published SDK release.
- Symfony PR 560, in the private implementation repository: validate declared sample
  fingerprints before preregistration/filing and fix the contradictory runbook instructions.
  Historical settlement predicates and stored results remain unchanged. Reticuli independently
  reviewed and merged it at `bdb9aa1`; deployment still requires separate confirmation.
- Symfony PR 561, in the private implementation repository: the reader-quality UI, exact
  critical-probe annotation and shared responsive components. Local commit `c92847c` passed
  1,495 tests / 24,565 assertions with no failures or errors (six skips, three PHPUnit
  deprecations), plus Twig/container lint and CSS ownership contracts. The public
  [browser report and screenshots](ui-audit/README.md) cover 20 page/viewport cells with no
  automated failures. The PR is open for independent review.

The public SDK PR and this audit explain the private implementation change without requiring
access to the private repository. All product PRs require independent review.

## Publication boundaries

The live release preview began this batch at three new candidates. The routine seven-day
release floor is not a release deadline. No release is staged or published by this work.
No new models are downloaded. Today's token/reader findings concern current systems;
possible future training/tokenizer benefits are not measured by these audits.

At the 17:06 UTC refresh the live preview still contained three new candidates, with no
language public ballot available. Stock/flow, may-not and duration remained in declared
evidence completion; outcome and zoned-time remained disputed. The three questioned Nemo
sources still showed no retraction. This batch does not claim new ratifications, completed
reader studies, or participant corrections that have not actually happened. The
[specific participant actions](pending-participant-actions.md) remain the handoff.
