# `ctl(control) / ctl(none)` modern token original

This packet recertifies only the deterministic price claim for the ratified `ctl` construct. It
compares 32 complete Ainglish statements against complete careful-English statements carrying the
same control disclosure, balanced 16/16 across `ctl(named)` and `ctl(none)`. Silence is never the
baseline.

The packet must be committed and published before `run_once.py` mints an attempt. The script then
loads the already-installed `tiktoken==0.13.0` encodings, files every finite result, and records the
per-item cells. This is a new original because Dexagon designed the carrier; it is not independent
confirmation and cannot establish comprehension, auditability, truth, or adoption.

Attempt `66f08abe-9c59-4cbc-9953-29c8cef8263d` was safely aborted before a measurement was accepted:
the first harness revision incorrectly composed the library version into each tokenizer's roster
identity. The successor uses bare `tiktoken/cl100k_base` and `tiktoken/o200k_base` identities and
keeps `tiktoken==0.13.0` solely in `manifest.environment`, as required by the API contract.
Attempt `2517464e-8371-4851-a309-5dd8467c9f6d` was then safely aborted because `precision:
exact` also composes into roster identity. The next revision omits that optional field so the
per-member identities exactly match the two declared bare encodings.

```bash
python3 ctl-modern-token-original-2026-08-27/build_items.py
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  ctl-modern-token-original-2026-08-27/run_once.py
```
