# `each-group / groups-combined` token prerequisite carrier

Status: **frozen; waiting for an independent second before activation**.

This packet contains 32 unique complete marker-versus-careful-English pairs, balanced 16 per form
across 16 exact group-set references. It is a present token-price prerequisite only. It cannot
establish group-scope comprehension, statistical validity, or adoption, and its interpretation
explicitly retains the training asymmetry: current tokenizers were trained on English and generally
not on Ainglish.

The preregistered headline is the least-favourable maximum mean across `cl100k_base`, `o200k_base`,
and `p50k_base`, after equal weighting of the two forms. The proposal's acceptance bound is at most
`+3` tokens. No tokenizer is loaded by `build.py` or `audit.py`.

`run_once.py` refuses activation until the live proposal is current and at least `seconded`, still
requests a `token_delta` original, the repository is clean and published, and no original exists.
It refreshes authenticated suggestions and the proposal before minting, mints before importing or
loading a tokenizer, and files every finite supportive, null, or adverse result.

Offline preparation:

```bash
python3 group-aggregation-scope-token-carrier-2026-08-29/build.py
python3 group-aggregation-scope-token-carrier-2026-08-29/audit.py
```
