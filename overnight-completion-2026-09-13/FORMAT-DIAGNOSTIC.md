# The earlier abort remains an abort

The frozen English-only format controls completed: **256/256** returned an
accepted, correct option, with no absent answer or transport exception. Each of
two already-cached readers saw 32 short, 32 compound, 32 punctuated and 32 long
option controls. The item design and runner were committed as `a937e16` before
the first call. No model weights were downloaded.

- [Frozen design](format-stress/design.json), SHA-256
  `08bf7130d00ed17559c54f56ac304e955c14dda7f2d74e0e48f0a57c0576762d`.
- [Bound reader identities and settings](format-stress/start.json).
- [All normalized diagnostic answers](format-stress/cells.json).
- [Result and usage receipt](format-stress/result.json).
- [Runner](format_stress.py): one pass, no retries, no answer-parser change.

These are simple answer-copy controls, **not language measurements, independent
replications, semantic qualification or a general reliability estimate**. Fixed
controls under deterministic settings do not justify an independent-trial
confidence bound on future model errors. The normalized journal is not a claim
to retain raw HTTP bodies.

The remain/departed panel previously produced an off-option response under its
zero-tolerance rule. This diagnostic did not reproduce that failure. The failure
could be specific to a harder prompt or interaction that these controls did not
exercise. We therefore have no evidence-backed reason to loosen the parser,
reinterpret its answer, rescue its result or repeat the exposed target bank.

The next defensible experiment needs a prospectively frozen, construct-independent
diagnostic that actually exercises the suspected condition. Any later language
attempt still requires wholly fresh targets, the source instrument, current
qualification and a valid contract. Passing this diagnostic supplies none of those
missing permissions. The current audit submitted **zero** language measurements.
