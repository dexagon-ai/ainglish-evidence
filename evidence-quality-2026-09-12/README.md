# Evidence quality and consequential decisions — 12 September 2026

## One-choice source correction

The original **6d4aeaa77d2c488a97bc50ce06f9551438afb92329d2280347fbe0c836230789**
contains 16 feasibility questions that offer both **no** and **no feasible assignment
exists** while keying only **no**. The question is whether any complete assignment
can satisfy the visible constraints. For these impossible assignments, both options
answer it correctly. The source item digest and manifest commitment verify.

The published raw journal has 32 cells on these questions. **Nine semantically
correct answers were scored false.** The report preserves the exact affected raw
answers and their originally published correctness bits. The narrow oracle derives
feasible assignments from the visible eligibility/capacity facts, not ledger gold.

This invalidates that instrument. The correction is author retraction, not choosing
a more favourable post-hoc official score. Retraction status and public receipts
will be linked after live readback. Other observations remain visible; no advantage,
confirmed harm, equivalence or new ratification follows from this audit.

### Scope and limitations

- The primary has 192 targets. Another 16 primary questions concern what *must*
  hold over an empty feasible set; these are explicitly unassessed because a
  natural-language/vacuity judgment is not silently supplied by a Python `all()`.
- The separate cold and reference originals each have 128 targets. The parser
  checks their 96 supported assignment frames and leaves 32 boundary frames
  unassessed. It did not find the duplicate-negative defect in those supported
  frames. This is not blanket semantic certification of those studies.
- Lemony's fresh remote replication **234095c3…** changes to three options in the
  feasibility stratum and a different reader population. Its 192 targets are
  unassessed by this deliberately narrow parser. Its empirical result remains a
  public observation even if retraction of the invalid source removes its
  settlement role. Filing acceptance does not certify scientific comparability.
- The oracle checks only named grammars. Comparator semantic equivalence remains
  un-certified; matching parsed facts is not proof of equivalent instructions.
- No new reader calls, official rescoring or policy changes were performed by
  this audit. This is the original author's audit, not independent confirmation.

Run from the repository root with an installed Ainglish SDK:

```sh
python -m unittest discover -s evidence-quality-2026-09-12 -p 'test_choice_audit.py' -v
```

`choice-audit.json` contains exact source identities, findings and raw answer
receipts. `measurements/` retains public measurement envelopes at audit time.

## Durable evidence

`artifact-index.json` and the report pin source URLs and exact archived bytes.
This includes Lemony's public rent and one-choice kits from x0.at, an expiring host.
Original source manifests are not rewritten, and an archival mirror is not a new
measurement or fresh replication population. Public reference inputs must never
be substituted for a new independent replication's fresh complete cases.

Only public source material is included here. Private DMs, credentials, personalised
work offers and executor inventories are not part of this directory.
