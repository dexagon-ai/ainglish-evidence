# `proxy(<M>)` fresh-input settlement replication v1

This package binds a genuinely prospective population to the unsettled complete-careful-English
original `bcc7b1d1…`. The source packet was publicly frozen at `889675e` before the original
attempt opened. It contains 96 scientific cases plus eight construct-free calibrations; the
original contains 60 plus eight.

`capture.py` opens the original public item artifact only inside an automated hash comparison. It
does not print or persist its answer-bearing text. The audit compares English messages, Ainglish
messages, complete arm pairs, and fully scored items. All four overlap counts must be zero. The
source itself remains byte-identical; `build.py` adds only the mechanical
`original-published-scalar` label to scientific rows, never to calibration.

The target original reported one pooled comprehension scalar against the complete careful-English
meaning. That scalar is the only settlement estimand here. Domain, question frame, wave, and
per-reader results are report-only diagnostics and cannot become favourable post-hoc gates.

Activation is intentionally blocked. The original used Qwen, Gemma, and Ornith. A confirmation
requires two lineages outside all three families, both qualified on the same previously unseen
ordinary-English holdout with sealed receipts. No reader or tokenizer call and no Ainglish attempt
is made by capture, build, or audit.

The two-phase freeze prevents a template from citing item bytes that were not public yet:

```bash
PYTHONPATH=/home/dexagon/codex/dexagon/scripts \
  /home/dexagon/codex/dexagon/.venv/bin/python \
  proxy-settlement-replication-v1-2026-08-29/capture.py

python3 proxy-settlement-replication-v1-2026-08-29/build.py
python3 proxy-settlement-replication-v1-2026-08-29/audit.py

# Commit and push the item phase, then bind that exact first-public commit.
python3 proxy-settlement-replication-v1-2026-08-29/build.py --item-commit <full-commit>
python3 proxy-settlement-replication-v1-2026-08-29/audit.py --require-template --write
```
