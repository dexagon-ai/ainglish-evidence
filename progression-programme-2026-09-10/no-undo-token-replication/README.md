# Frozen no-undo -4 token replication

Target: Lemony original `6a5e62a8e0a56127c91089f68451d525eed5957e067e91a58319919735a6c35c`.
Prepared commitment: `2341c2356ed339baf8dba28d4cb87d3ecb6d7068ad2e5e2cb7cf74b6b160691a`.

32 fictional pairs, 16 per required form, with balanced report/instruction shapes.
Each action is identical across its two arms. English uses the prospectively chosen
short genre (irreversibly/irrevocably or reversible via X), with the same named
holder, window and cost when present. No output-dependent variant selection.
Every can-undo fixture stipulates full immediate-pre-action restoration, not
partial restoration. The source contract's residual word "loss" is preserved
verbatim for identity; no loss slot or changed mapping is introduced.

Preparation verified zero exact pair and zero exact arm reuse from the source,
exact estimand-contract equality, comparison identity matched, 32-pair shape and
server preflight acceptance. No tokenizer counted these pairs during preparation.
The runner hash and plan were frozen before mint. Runtime uses the existing three
cached tokenizer vocabularies. This is a CPU cost measurement, not a reader panel,
permission to perform the fictional actions or evidence of future trained efficiency.

The run is performed once and all results retained. Agreement, disagreement, an
unresolved interval or failure are valid outcomes. The prerequisite's at_most-2
criterion and replication agreement are distinct questions. It cannot establish
the separate bare-English advantage or careful-English preservation claim.

The source/protocol read and latest author discussion were checked after no-undo
reached three seconds. Authenticated proposal snapshots used only for fresh-state
guards are intentionally excluded from this public directory.
