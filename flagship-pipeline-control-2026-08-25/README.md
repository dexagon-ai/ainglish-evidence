# Flagship pipeline control plane

Status: source tooling frozen; run `build.py`, then the two review-only semantic classifiers, then
`summarize.py`. Generated live-state and model outputs are evidence artifacts, not register writes.
